CAMBIOS v112f-venc-ui → v112g-fechaliq-prefill
===============================================
Fecha: 2026-06-18

UI / CAMPO "CALCULAR HASTA"
────────────────────────────
Prellenado con la fecha de hoy al cargar la app.

COMPORTAMIENTO:
  • Al abrir la app, fechaLiquidacion muestra la fecha actual (YYYY-MM-DD).
  • Si hay una sesión guardada con fecha distinta, applySession() la sobreescribe
    normalmente — no hay conflicto.
  • getFechaLiquidacion() no se modifica: sigue usando .value si hay fecha,
    o hoy como fallback (por si acaso el campo quedara vacío de otra forma).

CAMBIO (index.html — window.onload):
  Bloque IIFE añadido después de initCollapsibles():
    if (!fl.value) → pone yyyy-mm-dd de hoy.

ARCHIVOS MODIFICADOS: index.html, sw.js
SERVICE WORKER: v172f → v172g

