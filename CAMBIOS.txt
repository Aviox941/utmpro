==================================================================================
Fecha: 2026-06-20

CAMBIO: Texto de la casilla 2 en dupOverlay — "cuota mensual" → "última cuota pagada"
──────────────────────────────────────────────────────────────────────────────────
CONTEXTO
  En #dupOverlay ("Desde el último pago"), la casilla número 2 decía
  "Monto de la cuota mensual" — el mismo texto que usa quotaScreen (Paso 1
  del onboarding general). El usuario pidió diferenciarlo: en este overlay
  específico, el monto que se ingresa corresponde a la última cuota
  efectivamente pagada, no a la cuota mensual de referencia genérica.

SOLUCIÓN
  Cambiado únicamente dentro de #dupOverlay:
  - .dup-step__label (texto visible sobre el input, casilla "2")
  - aria-label del input #dupMontoCuota
  Ambos de "Monto de la cuota mensual" a "Monto de la última cuota pagada".
  quotaScreen (qs-input-label, Paso 1) no se tocó — el usuario confirmó
  explícitamente que el cambio era solo para este overlay.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html   (dup-step__label y aria-label del Paso 2 en dupOverlay)
- sw.js        (CACHE_NAME: pension-utm-v198)
- CAMBIOS.txt

==================================================================================

PROBLEMA
  #dupMontoCuota era type="number" nativo con step="0.01". Dos limitaciones
  propias de ese tipo de input causaban los bugs reportados:
  1. CLP: no admite separador de miles — al escribir "148.566" el navegador
     lo guardaba como "148566" (sin punto), porque para type="number" el
     punto solo puede ser el separador decimal, nunca de miles.
  2. UTM: el separador decimal de type="number" es siempre punto en HTML,
     sin importar el locale del navegador — tecleando coma ("2,33") el
     campo la rechazaba, y aunque se tecleara con punto, nunca se mostraba
     normalizada a coma (inconsistente con el resto de la app, que en
     campos UTM siempre usa coma — ver utmAmount, historicalDebtUtm,
     qsAmountInput).

  Este input nunca tuvo el mismo tratamiento que qsAmountInput
  (quotaScreen, Paso 1), que sí soluciona ambos casos vía
  qsFormatAmountInput() desde la v108-v109.

SOLUCIÓN
  Se cambió #dupMontoCuota de type="number" a type="text" con
  inputmode="decimal" (mismo patrón que qsAmountInput), y se agregó:
  - dupFormatMontoInput(input): saneo en vivo vía oninput. En UTM acepta
    '.' o ',' como separador (el teclado numérico de Android muestra
    punto) y normaliza siempre a coma, máx. 2 decimales. En CLP descarta
    todo lo no-numérico y formatea con separador de miles vía
    formatPlain() — exactamente la misma lógica que qsFormatAmountInput().
  - dupGetMontoValue(): convierte el valor visible (con coma decimal UTM
    o puntos de miles CLP) al número real, usada en dupCalcularDeuda() en
    vez del parseFloat() directo anterior (que ya no podría leer "2,33"
    ni "148.566" correctamente tras el cambio de formato).
  - csPrefillDesdeUltimoPago() ajustada para escribir en el mismo formato
    (coma decimal en UTM, formatPlain en CLP) que ahora produce
    dupFormatMontoInput(), manteniendo consistente la precarga del monto
    agregada en la entrega anterior.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html   (#dupMontoCuota: type="number"→"text", placeholder con
                 coma, oninput=dupFormatMontoInput; dupSelectUnidad: sin
                 referencias a step; nuevas dupFormatMontoInput()/
                 dupGetMontoValue(); dupCalcularDeuda() usa
                 dupGetMontoValue(); csPrefillDesdeUltimoPago() ajustada)
- sw.js        (CACHE_NAME: pension-utm-v197)
- CAMBIOS.txt

==================================================================================

CONTEXTO
  El fix anterior (separar la animación de opacity del fondo de .cs-screen)
  eliminó el bug de "fondo se vuelve transparente durante 280ms", pero el
  usuario reportó que el flash seguía ocurriendo, solo que más breve. Esto
  apuntaba a una causa distinta y más sutil.

CAUSA RAÍZ
  dupOverlay usa un slide-up al abrirse: parte en transform:translateY(100%)
  (completamente fuera del viewport, hacia abajo) y la animación
  dup-slide-up lo trae a translateY(0) en 320ms. openDupOverlay() le pone
  display:flex de inmediato, pero el transform sigue en 100% hasta que la
  animación avanza — es decir, aunque "técnicamente" ya está en el DOM como
  flex, sigue siendo invisible/fuera de pantalla durante el arranque del
  slide-up. En el flujo normal del dock esto es el efecto deseado (sube
  desde abajo sobre la app ya visible). Pero en la transición ENCADENADA
  desde calcTypeScreen, calcTypeScreen desaparece (display:none) en el
  mismo instante en que dupOverlay recién está empezando a subir desde
  fuera del viewport — durante esa ventana corta (los primeros ms del
  slide-up) no hay nada opaco cubriendo la pantalla, y se alcanza a ver el
  contenido real de la app por debajo. Eso era el flash "más rápido": ya no
  280ms de transparencia progresiva, sino el arranque del slide-up.

SOLUCIÓN
  openDupOverlay() ahora acepta un parámetro skipSlideIn. Cuando es true,
  en vez de animar desde translateY(100%), fuerza directamente
  transform:translateY(0) por estilo inline — el overlay cubre la pantalla
  desde el primer frame, sin ventana de transparencia. csOpenDesdeUltimoPago()
  llama openDupOverlay(true) en ambos puntos donde lo abre (con y sin
  #calcTypeScreen presente). El comportamiento del dock NO cambia: sigue
  usando el slide-up normal vía openDupOverlay() sin argumento.

  De paso se corrigió un bug que este cambio habría introducido: el dock
  conectaba la tarjeta con card.addEventListener('click', openDupOverlay)
  directo — addEventListener invoca el callback con el objeto Event como
  primer argumento, que es truthy y habría activado skipSlideIn por error
  en el dock también. Se envolvió en una función anónima que llama
  openDupOverlay() sin argumentos.

  closeDupOverlay() se ajustó para limpiar el transform inline al cerrar
  (el translateY(0) que pudo haber quedado de una apertura con
  skipSlideIn), evitando que compita con la animación normal de cierre
  (dup-slide-down).

ARCHIVOS MODIFICADOS
─────────────────────
- index.html   (openDupOverlay: nuevo parámetro skipSlideIn;
                 closeDupOverlay: limpia transform inline;
                 csOpenDesdeUltimoPago: openDupOverlay(true) en ambas
                 llamadas; conexión del dock envuelta para no heredar el
                 Event como skipSlideIn)
- sw.js        (CACHE_NAME: pension-utm-v196)
- CAMBIOS.txt

==================================================================================

DIAGNÓSTICO ANTERIOR INCOMPLETO
  El fix previo (subir z-index de .dup-overlay a 100000) era necesario pero
  insuficiente: no atacaba la causa real, por lo que el flash persistió.

CAUSA RAÍZ REAL
  .cs-screen es position:fixed; inset:0 con background:#ffffff, y la
  animación cs-fade-out (usada al cerrar la pantalla) animaba la opacity
  de ESE MISMO elemento — el que contiene tanto el fondo blanco como el
  contenido. Animar opacity sobre un elemento anima TODO lo que pinta ese
  elemento, incluido su propio background. Resultado: durante los 280ms
  de cs-fade-out, el background:#ffffff se iba haciendo progresivamente
  transparente, dejando ver perfectamente el contenido real de la app
  (detalle de cálculo, dock, etc.) que está debajo en el stacking —
  mezclado visualmente con calcTypeScreen desvaneciéndose encima. Esto es
  exactamente lo que se ve en la captura: el modal "¿Qué deseas calcular?"
  semitransparente flotando sobre la tabla de períodos.

  Este mismo patrón (animar opacity sobre el contenedor que también es el
  fondo opaco) existe igual en .qs-screen y .ws-screen, pero ahí nunca se
  notó porque lo que queda detrás al cerrarlas es otro overlay blanco del
  mismo flujo, no datos reales de la app. calcTypeScreen es la primera
  pantalla de este sistema que se cierra revelando directamente contenido
  con información real, por eso el bug —que ya existía estructuralmente—
  recién se hizo visible acá. No se tocó qs-screen/ws-screen: no fue parte
  de lo reportado y el protocolo del proyecto exige cambios quirúrgicos.

SOLUCIÓN
  Se separaron las dos capas: .cs-screen (fondo) ya NO tiene animación
  propia — su background:#ffffff permanece 100% opaco en todo momento.
  La animación (cs-inner-fade-in / cs-inner-fade-out) se movió a .cs-inner
  (el contenido real, max-width:430px centrado), que es semánticamente lo
  que debe desvanecerse/deslizarse, no el fondo que lo contiene.

  Como consecuencia, los listeners de animationend en closeCalcTypeScreen()
  y csOpenDesdeUltimoPago() ya no pueden escuchar sobre #calcTypeScreen
  (.cs-screen, que dejó de animar) — se corrigieron para escuchar sobre
  cs.querySelector('.cs-inner'), el elemento que ahora sí dispara el
  evento.

ARCHIVOS MODIFICADOS
─────────────────────
- styles.css   (cs-fade-in/out renombradas a cs-inner-fade-in/out y
                 movidas de .cs-screen a .cs-inner; .cs-screen sin
                 animación, fondo siempre opaco)
- index.html   (closeCalcTypeScreen, csOpenDesdeUltimoPago: animationend
                 escuchado sobre .cs-inner en vez de .cs-screen)
- sw.js        (CACHE_NAME: pension-utm-v195)
- CAMBIOS.txt

==================================================================================

PROBLEMA 1 — Se veía la interfaz de cálculo de fondo durante la transición
  Al tocar la tarjeta "Desde el último pago" en calcTypeScreen, durante la
  animación de cierre (~280ms) se veía mezclada/asomada toda la interfaz de
  resultados de la app (detalle de cálculo, dock, etc.) detrás del overlay
  semitransparente. Causa raíz: .dup-overlay tenía z-index:600 (heredado de
  cuando solo se abría desde el dock principal, donde no compite con ningún
  otro overlay fullscreen), mientras que welcomeScreen/quotaScreen/
  calcTypeScreen usan z-index 99998-99999. Al abrir dupOverlay mientras
  calcTypeScreen aún estaba desvaneciéndose (display:flex con opacity
  animando a 0), dupOverlay quedaba renderizado POR DEBAJO de calcTypeScreen
  en el stacking context, así que durante esos frames solo se veía
  calcTypeScreen haciéndose transparente sobre el fondo de la app — nunca
  llegaba a tapar nada porque estaba más abajo en el z-index.

  Adicionalmente, csOpenDesdeUltimoPago() registraba un segundo listener de
  animationend sobre #calcTypeScreen además del que ya registra
  closeCalcTypeScreen() (llamada internamente), generando una carrera de
  eventos sin orden garantizado entre "ocultar calcTypeScreen" y "abrir
  dupOverlay".

PROBLEMA 2 — Campo "Monto de la cuota mensual" en 0.00
  openDupOverlay() (función original, compartida con el dock) nunca recibió
  ni seteó un monto: solo hace display:flex + animación de entrada. Viniendo
  del dock esto es correcto (el usuario aún no ingresó nada), pero viniendo
  de calcTypeScreen el monto YA se había confirmado en quotaScreen — sin
  embargo openCalcTypeScreen() solo escribía ese valor como texto plano en
  el chip visual (#csQuotaMain/#csQuotaSecondary), sin guardarlo en ninguna
  variable recuperable.

SOLUCIÓN
  - .dup-overlay: z-index subido de 600 a 100000 (por encima de todo el
    sistema de onboarding), para que SIEMPRE quede visualmente por encima
    sin importar qué otra pantalla siga cerrándose detrás.
  - csOpenDesdeUltimoPago() reescrita: un solo listener de animationend
    (se removió la llamada a closeCalcTypeScreen() + el listener duplicado).
    Ese único callback aplica display:none + qsUnlockBodyScroll() y RECIÉN
    DESPUÉS llama openDupOverlay(), garantizando orden secuencial.
  - Nuevas variables de módulo _csUtmActual/_csClpActual/_csUnidadActual,
    pobladas en openCalcTypeScreen() con el monto y unidad reales (no solo
    el texto formateado) que el usuario confirmó en quotaScreen.
  - Nueva función csPrefillDesdeUltimoPago(), llamada justo después de
    openDupOverlay(): selecciona la unidad correcta vía dupSelectUnidad()
    (que internamente limpia el input) y RECIÉN DESPUÉS escribe el valor en
    #dupMontoCuota — respetando el formato con punto decimal que espera el
    input type="number" (toFixed(2) ya produce punto). Si no hay monto
    previo (_csUtmActual === 0, ej. entrando directo desde el dock sin pasar
    por el onboarding), no se toca el campo y se deja el placeholder 0.00
    de siempre.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html   (openCalcTypeScreen: guarda monto/unidad reales;
                 csOpenDesdeUltimoPago: un solo listener, orden secuencial;
                 nueva csPrefillDesdeUltimoPago())
- styles.css   (.dup-overlay: z-index 600 → 100000)
- sw.js        (CACHE_NAME: pension-utm-v194)
- CAMBIOS.txt

==================================================================================

CONTEXTO
  En la entrega anterior (calcTypeScreen, Paso 2 de 2) las 4 tarjetas de
  opción quedaron puramente visuales por decisión explícita. El usuario
  pidió ahora conectar específicamente la tarjeta "Desde el último pago"
  como disparador del mismo overlay (#dupOverlay) que ya usa la tarjeta
  equivalente del dock principal (.calc-mode-card--blue → openDupOverlay()),
  en vez de construir una pantalla de cálculo nueva.

SOLUCIÓN
  Se agregó onclick="csOpenDesdeUltimoPago()" a .cs-option--purple. Esta
  nueva función cierra calcTypeScreen con su fade-out normal (closeCalcType
  Screen) y, recién al terminar esa animación (animationend), llama a
  openDupOverlay() — la misma función que ya usa el dock. closeDupOverlay()
  no se modificó: al cerrar dupOverlay con la X, el usuario vuelve
  directamente a la app principal, igual que ocurre hoy desde el dock
  (decisión explícita del usuario, sin volver a calcTypeScreen).

  No fue necesario tocar el sistema de bloqueo de scroll: calcTypeScreen usa
  qsLockBodyScroll()/qsUnlockBodyScroll() (position:fixed sobre <body>) y
  dupOverlay usa su propio mecanismo aislado (body.style.overflow). Ambos
  se desactivan/activan de forma independiente sin pisarse, ya que
  qsUnlockBodyScroll() corre antes de que openDupOverlay() module overflow.

  Las otras 3 tarjetas (Entre fechas, Con lagunas, Pagos parciales) y el
  botón "Cambiar" del chip de cuota se mantienen sin conectar, sin cambios.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html   (cs-option--purple: onclick; nueva función csOpenDesdeUltimoPago())
- sw.js        (CACHE_NAME: pension-utm-v193)
- CAMBIOS.txt

==================================================================================

CONTEXTO
  Se integró un nuevo overlay de onboarding que aparece inmediatamente después
  de quotaScreen (Paso 1: cuota mensual de pensión), siguiendo el mismo patrón
  de transición que ya existía entre welcomeScreen → quotaScreen: la pantalla
  siguiente se muestra DEBAJO (z-index mayor, en este caso) ANTES de iniciar
  el fade-out de la anterior, evitando cualquier frame con la app de fondo
  visible. El diseño fue construido a partir de una imagen de referencia
  (mockup "¿Qué deseas calcular?") y de un snippet HTML/CSS de base provisto
  por el usuario, adaptado al sistema visual real de la app (paleta, tipografía,
  iconografía SVG lineal en vez de emojis, mismo header/progreso/animaciones
  que .qs-*) para que ambas pantallas del flujo sean indistinguibles en estilo.

QUÉ INCLUYE
  - #calcTypeScreen (clases cs-*): header con back-button, barra de progreso
    de 3 segmentos (2 activos) y label "Paso 2 de 2"; ícono de calculadora;
    título "¿Qué deseas calcular?" y subtítulo.
  - Chip "Cuota actual": muestra el monto recién ingresado en quotaScreen
    (UTM y su equivalente en CLP aproximado), con botón "Cambiar" que por
    ahora vuelve a quotaScreen (closeCalcTypeScreen). Los valores se
    completan dinámicamente en openCalcTypeScreen() a partir de lo
    confirmado en confirmQuotaScreen().
  - 4 tarjetas de opción (Desde el último pago / Entre fechas / Con lagunas /
    Pagos parciales), cada una con icono SVG de color semántico propio
    (morado/verde/naranja/azul) replicando los acentos de la imagen de
    referencia. Por decisión explícita del usuario, las tarjetas y el botón
    "Cambiar" son SOLO VISUALES en esta entrega — sin onclick de cálculo,
    para conectarse en una futura sesión.
  - Trust card inferior ("Cálculos 100% transparentes").

CAMBIOS DE FLUJO
  confirmQuotaScreen() ya no cierra el onboarding al confirmar: ahora calcula
  utmFinal/clpFinal según la unidad activa, llama openCalcTypeScreen(utm, clp)
  y luego closeQuotaScreen() — mismo orden que closeWelcomeScreen() usa con
  openQuotaScreen(). closeQuotaScreen() se ajustó para NO desbloquear el
  scroll del body si calcTypeScreen quedó abierto encima (mismo bloqueo de
  scroll se mantiene continuo entre las 3 pantallas del onboarding).

ARCHIVOS MODIFICADOS
─────────────────────
- index.html   (nuevo bloque #calcTypeScreen + funciones openCalcTypeScreen,
                 closeCalcTypeScreen; confirmQuotaScreen y closeQuotaScreen
                 ajustadas)
- styles.css   (nuevo bloque cs-* completo, reutilizando convenciones de qs-*)
- sw.js        (CACHE_NAME: pension-utm-v192)
- CAMBIOS.txt

==================================================================================

PROBLEMA
  Al elegir UTM en el selector de quotaScreen, el teclado numérico de Android
  (inputmode="decimal") muestra el punto "." como tecla de separador decimal,
  no la coma. La función qsFormatAmountInput() solo aceptaba ',' en su regex
  de saneo (/[^\d,]/g) y eliminaba cualquier '.' tecleado, por lo que
  "2.33" quedaba guardado como "233" (sin parte decimal).

SOLUCIÓN
  qsFormatAmountInput() ahora sanea aceptando tanto '.' como ',' y normaliza
  cualquier punto ingresado a coma antes de aplicar el límite de un solo
  separador y máximo 2 decimales. El resto del flujo (confirmQuotaScreen,
  qsToggleUnidad) ya esperaba formato con coma, así que no requirió cambios.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html   (qsFormatAmountInput: regex /[^\d.,]/g + replace('.', ','))
- sw.js        (CACHE_NAME: pension-utm-v191)
- CAMBIOS.txt

==================================================================================
Fecha: 2026-06-20

FIX: Botones "Comenzar" y "Continuar" alineados a misma altura + selector CLP/UTM funcional
──────────────────────────────────────────────────────────────────────────────────
PROBLEMA 1 — Altura de botones distinta
  En welcomeScreen el botón "Comenzar" fluía con el contenido (no sticky),
  por lo que su posición vertical variaba según cuánto contenido hubiera
  arriba. En quotaScreen el botón "Continuar" sí era sticky bottom, fijo
  siempre al fondo del viewport. Por eso no coincidían pese a ser pantallas
  back-to-back del mismo flujo de onboarding.

PROBLEMA 2 — Selector CLP/UTM decorativo
  El <span class="qs-currency-select"> en quotaScreen era puramente visual,
  sin onclick ni lógica: siempre mostraba "$ CLP" y el input solo aceptaba
  formato de pesos chilenos, sin posibilidad real de ingresar el monto en UTM.

SOLUCIÓN
  welcomeScreen se reestructuró igual que quotaScreen: ws-screen > ws-inner
  (min-height:100dvh, flex-column) > ws-content (scrollable, flex:1) +
  ws-footer (sticky bottom, fondo translúcido con blur, mismo padding/gap
  que qs-footer). La nota de pie, el botón Comenzar y los dots de
  paginación se movieron de ws-content a ws-footer. Ambos botones quedan
  ahora exactamente a la misma distancia del borde inferior del viewport,
  sin importar el contenido de cada pantalla.

  El selector de moneda se convirtió en un <button id="qsCurrencySelect">
  con ícono de chevron y onclick="qsToggleUnidad()". Esta función alterna
  entre CLP y UTM, reformateando en vivo el valor visible del input
  (convirtiendo con getUtmActualVal()) y cambiando su inputmode. Se separó
  qsFormatAmountInput() en dos ramas según la unidad activa: CLP usa
  formato entero con separador de miles (formatPlain), UTM usa formato
  decimal con coma (máx. 2 decimales), igual que el campo utmAmount del
  resto de la app. confirmQuotaScreen() ahora respeta la unidad activa:
  si es UTM, escribe directo en #utmAmount y llama updateFromUtm(); si es
  CLP, escribe en #clpAmount y llama updateFromClp() — sin tocar la unidad
  contraria innecesariamente.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html   (estructura welcomeScreen: ws-inner/ws-footer; botón
                 qsCurrencySelect; qsToggleUnidad(); qsFormatAmountInput()
                 y confirmQuotaScreen() con soporte CLP/UTM)
- styles.css   (.ws-inner nuevo, .ws-content scrollable, .ws-footer sticky,
                 .qs-currency-select como botón con cursor/font-family
                 heredados, .qs-currency-chevron)
- sw.js        (CACHE_NAME: pension-utm-v190)
- CAMBIOS.txt

CAMBIOS v2.0619.0232 → v2.0619.0233
==================================================================================
Fecha: 2026-06-20

FIX: Botón "Continuar" recortado + flash de fondo entre welcome y quota screen
──────────────────────────────────────────────────────────────────────────────────
PROBLEMA 1 — Footer del Quota Screen cortado/inalcanzable
  El botón "Continuar" del Quota Screen quedaba parcialmente fuera de la
  pantalla visible y no se podía hacer scroll para alcanzarlo completo.

CAUSA
  .qs-inner usaba min-height:100vh con .qs-body en flex:1, dentro de un
  contenedor de exactamente 100vh sin overflow propio. En navegadores
  móviles, 100vh no coincide con el viewport VISUAL real cuando la barra
  de direcciones está presente (Chrome la muestra/oculta dinámicamente):
  el layout completo medía más alto que el área visible, pero al ser
  exactamente 100vh tampoco generaba scroll — el footer quedaba "atrapado"
  fuera de vista sin forma de alcanzarlo.

SOLUCIÓN 1
  • .qs-inner: min-height pasa a 100dvh (dynamic viewport height, se ajusta
    en tiempo real al área visible real) con fallback a 100vh para
    navegadores sin soporte.
  • .qs-footer: ahora es position:sticky; bottom:0 con fondo blanco propio
    y padding-bottom que respeta env(safe-area-inset-bottom) — el botón
    "Continuar" queda SIEMPRE visible, sin depender de scroll, igual que
    un footer de app nativa.
  • .qs-body: overflow-y:auto propio + flex:1; min-height:0 — si el
    contenido del body es más alto de lo normal, scrollea internamente
    bajo el footer sticky, en vez de empujarlo fuera de la pantalla.

PROBLEMA 2 — Flash de la app de fondo entre welcomeScreen → quotaScreen
  Durante la transición (fade-out del welcome, fade-in del quota) se
  alcanzaba a ver brevemente la interfaz de la calculadora detrás.

CAUSA
  closeWelcomeScreen() ocultaba primero welcomeScreen (display:none) y
  solo DESPUÉS llamaba a openQuotaScreen() (display:flex). Aunque ambas
  líneas son síncronas, el cambio de display de un elemento opaco a otro
  puede generar un frame intermedio sin ningún overlay pintado — ahí se
  asoma la app de fondo durante una fracción de segundo.

SOLUCIÓN 2
  Se invirtió el orden: ahora openQuotaScreen() se llama PRIMERO (quota
  queda pintado y visible, pero oculto bajo welcomeScreen gracias a que
  welcomeScreen tiene z-index mayor: 99999 vs 99998) y solo entonces se
  inicia el fade-out de welcomeScreen. El usuario ve a welcomeScreen
  desvanecerse revelando quotaScreen ya listo debajo — nunca hay un frame
  sin overlay opaco cubriendo la pantalla.

ARCHIVOS MODIFICADOS
  • styles.css  → .qs-inner (100dvh), .qs-body (overflow-y:auto, flex:1,
                  min-height:0), .qs-footer (sticky bottom + safe-area).
  • index.html  → closeWelcomeScreen() reordenado: openQuotaScreen() ahora
                  se llama antes de iniciar el fade-out del welcomeScreen.
                  app-version → v2.0619.0233.
  • sw.js       → CACHE_NAME → 'pension-utm-v189'.

SIN CAMBIOS EN LÓGICA DE LOGIN, CÁLCULO NI MARKUP HTML DE LOS OVERLAYS
  No se modificó ningún archivo de markup (solo CSS) ni la lógica de
  onAuthStateChange, confirmQuotaScreen(), ni los helpers de bloqueo de
  scroll introducidos en la versión anterior.

==================================================================================

CAMBIOS v2.0619.0231 → v2.0619.0232
==================================================================================
Fecha: 2026-06-20

FIX: Scroll del welcome/quota screen se filtraba a la app de fondo
──────────────────────────────────────────────────────────────────────────────────
PROBLEMA
  Al hacer scroll dentro del Welcome Screen o del Quota Screen, el scroll
  se "filtraba" hacia el body de la app detrás (efecto rubber-band típico
  de overlays fullscreen sin bloqueo de scroll): se llegaba a ver la
  interfaz de la calculadora (dock fijo, tarjetas, etc.) detrás del overlay
  durante el gesto, en vez de quedar completamente bloqueada.

CAUSA
  Ambos overlays (.ws-screen / .qs-screen) usan position:fixed + scroll
  interno propio, pero el <body> de la página real no tenía ningún
  bloqueo de scroll mientras el overlay estaba abierto — seguía siendo
  scrolleable de forma independiente, lo que en navegadores móviles
  produce el efecto visual de "dos capas moviéndose a la vez".

SOLUCIÓN
  Se agregaron dos helpers, qsLockBodyScroll() / qsUnlockBodyScroll(), que
  fijan el body en position:fixed (anclado al scrollY actual) mientras
  cualquiera de los dos overlays está visible, y lo restauran exactamente
  en la misma posición al cerrar el último de ellos. Es la técnica estándar
  para bloquear scroll de fondo sin saltos visuales en iOS/Android.

  Puntos de conexión:
  • Al mostrarse el welcomeScreen (SIGNED_IN en onAuthStateChange) → lock.
  • Al abrirse el quotaScreen (justo después del welcome) → confirma el lock.
  • Al cerrarse el quotaScreen (Continuar / "Ahora no" / volver) → unlock,
    restaurando el scroll del body a su posición original.

  El overlay welcomeScreen no hace unlock por sí mismo porque siempre
  encadena la apertura de quotaScreen; el unlock ocurre en el único punto
  donde finalmente no queda ningún overlay fullscreen abierto.

ARCHIVOS MODIFICADOS
  • index.html  → nuevas funciones qsLockBodyScroll()/qsUnlockBodyScroll();
                  conectadas en el bloque SIGNED_IN, openQuotaScreen() y
                  closeQuotaScreen(). app-version → v2.0619.0232.
  • sw.js       → CACHE_NAME → 'pension-utm-v188'.

SIN CAMBIOS EN LÓGICA DE LOGIN, CÁLCULO NI MARKUP DE LOS OVERLAYS
  No se modificó el HTML ni el CSS de welcomeScreen/quotaScreen, ni
  onAuthStateChange más allá de la línea que ya disparaba el display:flex.

==================================================================================

CAMBIOS v2.0619.0230 → v2.0619.0231
==================================================================================
Fecha: 2026-06-20

FIX CRÍTICO: ID duplicado "welcomeScreen" — pantalla antigua reaparecía
──────────────────────────────────────────────────────────────────────────────────
PROBLEMA REAL (no era caché)
  Volvía a aparecer la pantalla antigua "⚡ CASO NUEVO" con input "Nombre o
  carátula del caso...", botón "Crear →" y botón balanza ⚖️ — una pantalla
  que ya había sido eliminada del flujo de la app hace tiempo.

CAUSA RAÍZ
  Existían DOS elementos con id="welcomeScreen" cargados simultáneamente
  en el DOM:
    1. El welcomeScreen ACTUAL (overlay nuevo, clases .ws-*), definido al
       final de index.html.
    2. Un welcomeScreen VIEJO Y HUÉRFANO que seguía completo dentro de
       modals.html — con su propio markup "Caso nuevo", input
       #welcomeNombreInput, botones #welcomeBtnRapido / #welcomeBtnJudicial,
       y onclick/oninput apuntando a funciones que YA NO EXISTEN en ningún
       archivo del proyecto (crearCasoDesdeWelcome, abrirCasoJudicialDesdeWelcome,
       checkWelcomeBtn) — código muerto de una versión anterior que nunca
       se limpió al introducir la pantalla de bienvenida actual.

  index.html carga modals.html de forma asíncrona vía fetch() y vuelca su
  HTML completo dentro de <div id="modals-container"></div>, ubicado MUY
  arriba en el documento (línea ~1556) — antes del welcomeScreen nuevo
  (línea ~7988). Una vez que esa inyección async completaba, el
  welcomeScreen viejo terminaba apareciendo ANTES en el orden del DOM.
  document.getElementById('welcomeScreen') siempre devuelve el primer
  elemento con ese id en orden de documento, así que CUALQUIER referencia
  a #welcomeScreen en el código (incluida la lógica nueva en
  onAuthStateChange / closeWelcomeScreen()) terminaba operando sobre el
  modal viejo en vez de sobre el overlay actual — explicando por qué la
  pantalla "correcta" nunca se mostraba y la antigua sí.

SOLUCIÓN
  Se eliminó por completo el bloque welcomeScreen viejo de modals.html
  (markup completo: card "Caso nuevo", input, botones, lista de casos
  existentes welcomeCasosWrap/welcomeCasosList) junto con sus referencias
  a funciones inexistentes. No se tocó ningún otro modal de modals.html
  (ayudaModal, casosManagerModal, casoSnapshotModal quedan intactos).
  Ahora solo existe UN elemento con id="welcomeScreen" en todo el proyecto
  (el overlay actual definido en index.html), eliminando la ambigüedad.

ARCHIVOS MODIFICADOS
  • modals.html → eliminado bloque #welcomeScreen viejo/huérfano
                  (código muerto desde una versión anterior).
  • index.html  → app-version → v2.0619.0231 (sin otros cambios).
  • sw.js       → CACHE_NAME → 'pension-utm-v187'.

SIN CAMBIOS EN LÓGICA DE LOGIN, CÁLCULO NI QUOTA SCREEN
  No se modificó onAuthStateChange, closeWelcomeScreen(), el Quota Screen,
  ni ningún otro modal o función. Este fix es exclusivamente la
  eliminación de markup duplicado/huérfano.

==================================================================================

CAMBIOS v2.0619.0229 → v2.0619.0230
==================================================================================
Fecha: 2026-06-20

FIX CRÍTICO: Service Worker servía pantalla antigua en deploys fuera de /utmpro/
──────────────────────────────────────────────────────────────────────────────────
PROBLEMA
  En el deploy de Vercel (raíz "/", sin subpath) seguía apareciendo una
  pantalla "CASO NUEVO" antigua, ya eliminada hace tiempo del código fuente.
  No era una versión distinta del proyecto: el HTML real servido al navegador
  estaba desincronizado del repo/ZIP actual.

CAUSA RAÍZ
  sw.js tenía hardcodeado:
      const BASE = '/utmpro/';
  y el fetch handler usaba:
      if (!url.pathname.startsWith(BASE) && url.origin === self.location.origin) return;
  En Vercel el sitio se sirve desde la raíz ("/"), así que NINGÚN pathname
  empezaba con "/utmpro/". Esa condición se cumplía siempre y el handler
  hacía return antes de llegar a event.respondWith(...): el Service Worker
  dejaba de interceptar absolutamente todas las requests del propio sitio.
  Sin esa intercepción, el SW nunca aplicaba su estrategia network-first ni
  refrescaba el caché — el navegador quedó sirviendo un index.html viejo
  cacheado de un registro anterior del SW (o del HTTP cache nativo), de ahí
  la pantalla "CASO NUEVO" que ya no existe en el código fuente.

SOLUCIÓN
  BASE ya no se hardcodea: se calcula dinámicamente desde el scope real de
  registro del Service Worker:
      const BASE = new URL(self.registration.scope).pathname;
  - En GitHub Pages (scope "https://aviox941.github.io/utmpro/") → BASE
    sigue siendo "/utmpro/", comportamiento idéntico al anterior.
  - En Vercel u otro deploy en raíz (scope ".../") → BASE pasa a ser "/",
    por lo que el SW vuelve a interceptar y aplicar network-first sobre
    todo el sitio, igual que estaba pensado.
  El fallback offline (caches.match(BASE + 'index.html')) usa el mismo
  valor dinámico, por lo que sigue siendo coherente en ambos entornos.

ARCHIVOS MODIFICADOS
  • sw.js       → BASE calculado vía self.registration.scope en vez de
                  string hardcodeado '/utmpro/'. CACHE_NAME → 'pension-utm-v186'.
  • index.html  → app-version → v2.0619.0230 (sin otros cambios).

SIN CAMBIOS EN LÓGICA DE LOGIN, CÁLCULO NI QUOTA SCREEN
  Este fix es exclusivamente del Service Worker (estrategia de caché /
  scope). No se tocó ningún otro archivo ni lógica de la app.

==================================================================================

CAMBIOS v2.0619.0228 → v2.0619.0229
==================================================================================
Fecha: 2026-06-19

NUEVO: Quota Screen overlay (pantalla "¿Cuál es tu cuota mensual de pensión?")
──────────────────────────────────────────────────────────────────────────────────
QUÉ SE AGREGÓ
  Pantalla overlay fullscreen (#quotaScreen, position:fixed; inset:0;
  z-index:99998) que se muestra inmediatamente después de cerrar el
  Welcome Screen (encadenada desde closeWelcomeScreen()), siguiendo el
  diseño de referencia entregado por el usuario.

  NOTA IMPORTANTE: esta versión re-aplica el feature de Quota Screen sobre
  la base correcta de v2.0619.0228, la cual ya incluye el fix de
  "Welcome Screen aparece después del login, no antes" (SIGNED_IN /
  INITIAL_SESSION en onAuthStateChange). Una entrega previa del ZIP base
  (sin ese fix) había recibido el Quota Screen por error; esta versión
  corrige eso construyendo sobre la base correcta. El disparador de la
  welcome screen (y por tanto del quota screen, que se abre justo después)
  sigue siendo exclusivamente el evento SIGNED_IN de onAuthStateChange —
  no se modificó esa lógica de login.

  Elementos implementados (clases con prefijo .qs-* para no colisionar
  con ningún CSS existente):
  • Header con botón "volver", barra de progreso (5 segmentos, 2 activos)
    y etiqueta "Paso 1 de 2"
  • Ícono calendario + moneda (SVG inline, mismo estilo visual que el
    Welcome Screen)
  • Título "¿Cuál es tu cuota mensual de pensión?" y subtítulo explicativo
  • Input card con selector "$ CLP" (visual, no funcional) + campo de
    monto con formato de miles en vivo (reutiliza formatPlain())
  • Security card informativa ("Tus datos están seguros")
  • Footer con botón "Continuar" y link "Ahora no, lo haré después"

  Animación de entrada/salida fade idéntica en estructura a la del
  Welcome Screen (@keyframes qs-fade-in / qs-fade-out), pero con su
  propio namespace para no compartir clases.

CONEXIÓN CON EL FORMULARIO PRINCIPAL (variable global existente)
  Al presionar "Continuar", el monto ingresado se escribe directamente
  en el campo real #clpAmount y se invoca updateFromClp() + saveSession() —
  exactamente el mismo camino que ya usa el campo de "Valor cuota en
  pesos" del formulario. Esto:
    - Activa automáticamente el modo de cálculo "clp"
    - Sincroniza el equivalente en UTM (#utmAmount)
    - Deja el valor disponible para getMonthlyValues() en toda la app
      (incluye los cálculos de "Calcular desde el último pago", LAV
      modo libre, etc.)
  El valor queda totalmente editable después: no se fija ni se bloquea,
  el usuario puede cambiarlo en cualquier momento desde el formulario
  normal.

  El link "Ahora no, lo haré después" y el botón "volver" solo cierran
  el overlay (closeQuotaScreen()) sin tocar ningún campo ni variable.

ARCHIVOS MODIFICADOS
  • index.html  → nuevo bloque #quotaScreen + funciones JS
                  (qsFormatAmountInput, openQuotaScreen, closeQuotaScreen,
                  confirmQuotaScreen); closeWelcomeScreen() ahora encadena
                  la apertura de quotaScreen tras su animación de cierre.
                  app-version → v2.0619.0229.
  • styles.css  → nuevo bloque "QUOTA SCREEN (qs-*)" agregado al final
                  del archivo, sin modificar ninguna regla existente.
  • sw.js       → CACHE_NAME incrementado a 'pension-utm-v185' para
                  forzar actualización de cache en PWA.

SIN CAMBIOS EN LÓGICA DE LOGIN NI DE CÁLCULO
  No se modificó onAuthStateChange, hideLoginScreen/showLoginScreen, ni
  calculate()/getMonthlyValues(), ni ningún onclick/handler existente
  más allá de encadenar la apertura del nuevo overlay al final de
  closeWelcomeScreen().

==================================================================================

CAMBIOS v2.0619.0227 → v2.0619.0228
==================================================================================
Fecha: 2026-06-19

FIX: Welcome Screen aparece después del login, no antes
──────────────────────────────────────────────────────────────────────────────────
PROBLEMA
  La welcome screen se inicializaba con un IIFE al cargar la página, antes
  de que onAuthStateChange confirmara la sesión. Esto hacía que apareciera
  por encima del login screen en vez de mostrarse tras autenticarse.

SOLUCIÓN
  1. El IIFE inicial ahora siempre oculta la welcome screen (display:none).
     Ya no la muestra al cargar — eso lo delega a onAuthStateChange.
  2. En onAuthStateChange, cuando event === 'SIGNED_IN' (login manual exitoso)
     y sessionStorage 'ws_done' no está marcado, se hace display:flex sobre
     el #welcomeScreen (ya el loginScreen está oculto gracias a hideLoginScreen).
  3. En INITIAL_SESSION (sesión ya existente, usuario regresa a la app) NO
     se muestra la welcome screen — el usuario ya está logueado, va directo al app.
  sessionStorage 'ws_done' sigue controlando que no aparezca dos veces
  en la misma sesión del navegador.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html  (IIFE ws + bloque SIGNED_IN en onAuthStateChange; app-version v2.0619.0228)
- sw.js       (CACHE_NAME: pension-utm-v184)
- CAMBIOS.txt

CAMBIOS v2.0619.0226 → v2.0619.0227
==================================================================================
Fecha: 2026-06-19

NUEVO: Welcome Screen overlay (onboarding)
──────────────────────────────────────────────────────────────────────────────────
QUÉ SE AGREGÓ
  Pantalla de bienvenida fullscreen (position:fixed; inset:0; z-index:99999)
  fiel al diseño de referencia. Se muestra al abrir la app por primera vez
  en la sesión (sessionStorage 'ws_done'). Al tocar "Comenzar" se cierra
  con animación fade-out y no vuelve a aparecer hasta nueva sesión.

  Elementos implementados:
  • Fondo: gradiente lavanda (linear-gradient 165deg #edeaff → #f5f6fb)
  • Logo: SVG inline con documento+símbolo$+persona (ícono bicolor navy/verde),
    fondo blanco con sombra suave, border-radius 28px
  • Título: "Bienvenido a" en #1a1d40, "Pension" en #5b4ff7 (morado),
    "App" en #22c55e (verde) — font-weight 800, 38px
  • Subtítulo: 17px #667085
  • Features card: fondo blanco, border-radius 24px, 4 filas con separadores
    - Ícono SVG propio para cada feature (no emojis):
      🟣 Cálculos precisos (barras, fondo #f0ecff)
      🟢 Resultados en segundos (calendario con check, fondo #e8faf0)
      🟠 100% transparente (barras de stats, fondo #fff8ec)
      🔵 Seguro y privado (escudo con candado, fondo #eef3ff)
  • Nota de pie: ícono escudo morado + texto "Basado en normativa vigente..."
  • Botón "Comenzar →": gradiente morado, 60px alto, sombra accent, SVG flecha
  • Dots de paginación: dot activo ancho (22px) morado, inactivos grises

  CSS completamente aislado (prefijo .ws-*) al final de styles.css.
  JS inline al final de index.html: closeWelcomeScreen() con animación.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html  (#welcomeScreen + <script> ws + app-version v2.0619.0227)
- styles.css  (bloque .ws-* al final)
- sw.js       (CACHE_NAME: pension-utm-v183)
- CAMBIOS.txt


==================================================================================
Fecha: 2026-06-19

FIX: Radio UTM/CLP congelado + centrado en tablet
──────────────────────────────────────────────────────────────────────────────────
PROBLEMAS
  1. Al seleccionar CLP en el overlay "Desde el último pago", el radio se
     congelaba y no permitía volver a seleccionar UTM.
  2. En tablet el contenido del overlay se veía muy estirado/descentrado,
     sin respetar el ancho máximo de la referencia visual.

SOLUCIONES
  1. BUG RADIO: Se reemplazaron los <label>+<input type="radio"> por
     <button type="button"> con onclick="dupSelectUnidad('utm'/'clp')".
     Los inputs radio ocultos (opacity:0, pointer-events:none) causaban
     comportamiento inconsistente en Android al hacer click repetido sobre
     el mismo label. La nueva implementación usa variable de estado JS
     (_dupUnidad) en lugar de depender del estado nativo del input,
     eliminando por completo la causa del congelamiento.

  2. CENTRADO TABLET: Se reemplazó el bloque @media (min-width:768px) que
     intentaba centrar el overlay completo (con translateX) por un enfoque
     de max-width en el contenido interior: el overlay sigue siendo inset:0
     pero el body usa display:flex + align-items:center con max-width:520px
     sobre los elementos hijos. Header y footer también respetan el límite.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html  (radio group → buttons; dupSelectUnidad(); _dupUnidad state; v0226)
- styles.css  (dup-radio como button; @media centrado por contenido)
- sw.js       (CACHE_NAME: pension-utm-v182)
- CAMBIOS.txt


==================================================================================
Fecha: 2026-06-19

NUEVO: Pantalla fullscreen "Desde el último pago"
──────────────────────────────────────────────────────────────────────────────────
QUÉ SE AGREGÓ
  Al tocar la tarjeta azul "Desde el último pago" de la barra fija inferior,
  se abre un overlay fullscreen (position:fixed; inset:0; z-index:600) que
  cubre toda la pantalla con animación slide-up estilo navegación nativa iOS/
  Android. No es un modal emergente: ocupa la pantalla completa y tiene su
  propio header con botón ← volver que lo cierra con slide-down.

  Contenido del overlay:
  • Hero card: ícono de calendario en fondo lavanda + título + subtítulo
  • Paso 1: radio group UTM / CLP con indicador animado y borde morado activo
  • Paso 2: input de monto con prefijo UTM/$, fuente 22px, sin flechas nativas
  • Paso 3: date picker nativo encubierto con display formateado "DD / MM / YYYY"
            + hint "Calcularemos desde el día siguiente a esta fecha"
  • Footer fijo: botón CTA morado "Calcular deuda actual" con safe-area-inset

  El overlay vive fuera del #app-container (justo antes de </body>).
  CSS completamente nuevo y aislado (prefijo .dup-*) al final de styles.css.
  JS en bloque <script> al final de index.html: openDupOverlay(),
  closeDupOverlay(), dupOnUnidadChange(), dupTriggerDatePicker(),
  dupOnFechaChange(), dupCalcularDeuda() (con TODO para conectar al motor).
  La tarjeta azul .calc-mode-card--blue recibe addEventListener click en
  DOMContentLoaded implícito mediante IIFE al final del script.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html  (overlay #dupOverlay + <script> DUP + app-version v2.0619.0225)
- styles.css  (bloque .dup-* al final)
- sw.js       (CACHE_NAME: pension-utm-v181)
- CAMBIOS.txt


==================================================================================
Fecha: 2026-06-19

AJUSTE: "¿Cómo deseas calcular?" pasa a barra fija inferior compacta
──────────────────────────────────────────────────────────────────────────────────
PROBLEMA
  Con tarjetas de 170×140px en fila con scroll horizontal, la 4ª tarjeta
  ("Pagos parciales") quedaba cortada/oculta fuera del viewport, y al
  hacer scroll vertical la sección completa se perdía de vista (era parte
  del flujo normal de contenido).

SOLUCIÓN
  La sección ahora es position:fixed al fondo de toda la pantalla
  (bottom:0, z-index:250), por lo que las 4 tarjetas están siempre
  visibles sin importar el scroll. Tarjetas reducidas a un grid de 4
  columnas iguales (antes: fila con scroll horizontal), cada una de
  ~64px de alto con ícono de 24px y título de una sola línea (se quitó
  el salto de línea y la flecha "›" para que entre todo sin recortes).

  Para que la barra fija no tape el contenido al llegar al final del
  scroll, se aumentó el padding-bottom de #app-container a 120px
  (antes pb-20 = 80px). El ancho interno de la barra (.calc-mode-
  section__inner) se sincronizó con los mismos breakpoints responsivos
  de #app-container para quedar alineada en tablet/desktop.

  Se eliminó el uso de .no-scrollbar (ya no hay scroll horizontal: es
  un grid de 4 columnas que siempre entra completo). Sigue sin tener
  acción funcional (onClick) — solo presentación visual.


==================================================================================
Fecha: 2026-06-19

NUEVO: Sección visual "¿Cómo deseas calcular?" (estilo fintech/Nubank)
──────────────────────────────────────────────────────────────────────────────────
QUÉ SE AGREGÓ
  Bloque puramente decorativo con 4 tarjetas (Desde el último pago, Entre
  fechas, Con lagunas, Pagos parciales), insertado inmediatamente debajo de
  la tarjeta "Total Adeudado" y antes de "Detalle del cálculo". Scroll
  horizontal en móvil, sin scrollbar visible, gap 16px, tarjetas de
  170×140px con radio 24px y gradientes de color por variante (azul,
  violeta, verde, rosa).

  Las tarjetas NO tienen acción funcional todavía (sin onClick real): son
  solo presentación visual a la espera de definir a qué flujo conectar cada
  una. Se evitó inventar un mapeo arbitrario hacia secciones existentes
  (LAV, Deuda Histórica, Abonos, Pagos Parciales) para no generar
  comportamiento inesperado.

  CSS 100% nuevo y aislado (prefijo .calc-mode-*) agregado al final de
  styles.css. Reutiliza la utilidad .no-scrollbar ya existente. No se
  modificó ninguna clase, función JS, hook, contexto, lógica de cálculo,
  IndexedDB ni estado global. Cambio puramente de UI.

CAMBIOS v2.0619.0202 → v2.0619.0222
==================================================================================
Fecha: 2026-06-19

FIX: Eliminado wait de IDB que demoraba/abortaba la restauración en frío
──────────────────────────────────────────────────────────────────────────────────
PROBLEMA
  Tras corregir el ReferenceError de abonosLav, la restauración funcionaba
  bien al iniciar sesión, pero seguía fallando al cerrar la app por completo
  (deslizar fuera de "recientes" en Android, matando el proceso) y volver
  a abrirla — tanto en PWA como en pestaña de navegador normal.

  La causa: sbLoadCasos() (FASE 1) hacía
    await Promise.race([window._idbRestorePromise, timeout 800ms])
  ANTES de leer localStorage. Este wait se agregó para proteger contra el
  caso de Android limpiando localStorage, pero en arranque en frío real
  (proceso recién matado) el restore de IndexedDB puede demorar más de lo
  esperado, y bloquear ahí FASE 1 retrasaba — o en combinación con otros
  timeouts de la cadena de arranque, terminaba dejando la pantalla en
  blanco sin restaurar el caso.

SOLUCIÓN
  Se eliminó el wait explícito. El restore de IDB → localStorage sigue
  ejecutándose igual, pero corre en paralelo desde el script temprano del
  <head> (independiente de sbLoadCasos) sin bloquear la UI. FASE 1 vuelve
  a leer localStorage directamente y de inmediato, igual que en la versión
  de referencia comprobada (línea base previa a esta serie de cambios).

  El respaldo en IndexedDB de pension_utm_auth, pension_utm_last_caso,
  pension_utm_casos_index_v1 y pension_utm_caso_v1_* sigue intacto y activo
  (vía el monkey-patch de localStorage.setItem/removeItem y el storage
  adapter _sbStorage) — solo se quitó el bloqueo síncrono en sbLoadCasos.

ARCHIVOS MODIFICADOS
─────────────────────
- index.html  (sbLoadCasos FASE 1 — quitado await idbRestorePromise; app-version)
- sw.js       (CACHE_NAME: pension-utm-v178)
- CAMBIOS.txt

==================================================================================
HISTORIAL RECIENTE
==================================================================================
v2.0619.0222 : FIX — quitado wait de IDB que retrasaba/rompía restauración en frío
v2.0619.0202 : FIX REAL — abonosLav sin declarar rompía restauración de sesión
v2.0619.0147 : Auto-restore de caso post-sync Supabase tras quitar welcomeScreen
==================================================================================
