================================================================================
INSTRUCCIONES DE VERSIONADO
================================================================================
- Este archivo se SOBRESCRIBE en cada entrega.
- Archivos a actualizar: index.html (meta + label), sw.js (CACHE_NAME), CAMBIOS.txt

================================================================================
VERSIÓN ACTUAL
================================================================================
Fecha         : 12-06-2026
App version   : 2.0612.v69d  ← diagnóstico — panel visible desde inicio, TEMPORAL
SW cache      : pension-utm-v137
Archivos mod  : index.html, sw.js

================================================================================
PROPÓSITO DE ESTA VERSIÓN
================================================================================

v68d mostró que los logs de sbLoadCasos (LOAD[SB1] en adelante) no aparecen.
Causa: el panel _dbgPanel se agrega al body en DOMContentLoaded, pero
onAuthStateChange + sbLoadCasos se disparan ANTES de DOMContentLoaded.
_earlyDbgAdd sí captura los mensajes en el elemento p, pero el panel
no estaba visible (display:none) así que no se veían al abrirse.

FIX DE DIAGNÓSTICO: panel creado con display:block desde el IIFE inicial
(antes de cualquier evento). Así todos los logs desde el primer ms son visibles.

Esta versión debería mostrar LOAD[SB1..SB5] y LOAD[A/B/C] completos.

================================================================================
