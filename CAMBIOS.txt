================================================================================
INSTRUCCIONES DE VERSIONADO
================================================================================
- Este archivo se SOBRESCRIBE en cada entrega. No se acumulan versiones aquí.
- Cada modificación debe actualizar:
    1. index.html  → <meta name="app-version" content="2.MMDD.vXX">
    2. index.html  → <p id="appVersionLabel"> con el mismo valor
    3. sw.js       → CACHE_NAME = 'pension-utm-vNNN'  (incrementar NNN)
    4. Este archivo CAMBIOS.txt con el detalle de qué se modificó
- La letra/sufijo de versión (ej. v54H) debe coincidir siempre con el nombre
  del zip entregado (ej. v54H.zip).

================================================================================
VERSIÓN ACTUAL
================================================================================
Fecha         : 12-06-2026
App version   : 2.0612.v54H   (meta + appVersionLabel en index.html)
SW cache      : pension-utm-v120   (sw.js)
Archivos mod  : index.html, sw.js

================================================================================
CAMBIOS EN ESTA ENTREGA
================================================================================

1. FIX ALINEACIÓN BOTÓN CASOS (index.html)
   - Eliminado position:absolute del botón — causaba que se posicionara fuera
     del flujo y sobresaliera del margen izquierdo.
   - Nuevo layout: flex row con botón real a la izquierda + spacer invisible
     idéntico a la derecha → cards geométricamente centradas en cualquier
     pantalla sin depender de breakpoints.
   - Padding del ticker: 12px izq/der, alineado con header-row-main.
   - Tickers no modificados.
   - Sin cambios a lógica de cálculo, eventos JS ni contenido PDF.

================================================================================
