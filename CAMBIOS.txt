================================================================================
INSTRUCCIONES DE VERSIONADO
================================================================================
- Este archivo se SOBRESCRIBE en cada entrega. No se acumulan versiones aquí.
- Cada modificación debe actualizar:
    1. index.html  → <meta name="app-version" content="2.MMDD.vXX">
    2. index.html  → <p id="appVersionLabel"> con el mismo valor
    3. sw.js       → CACHE_NAME = 'pension-utm-vNNN'  (incrementar NNN)
    4. Este archivo CAMBIOS.txt con el detalle de qué se modificó
- La letra/sufijo de versión (ej. v58) debe coincidir siempre con el nombre
  del zip entregado (ej. v58.zip).

================================================================================
VERSIÓN ACTUAL
================================================================================
Fecha         : 12-06-2026
App version   : 2.0612.v58   (meta + appVersionLabel en index.html)
SW cache      : pension-utm-v125   (sw.js)
Archivos mod  : export.js, modals.html, sw.js, index.html

================================================================================
CAMBIOS EN ESTA ENTREGA
================================================================================

1. FIX MENÚ DESCARGA NO SE CIERRA (export.js, modals.html)
   - Al presionar PDF o Excel, el menú flotante no se cerraba tras la descarga.
   - Causa: los botones llamaban generarPDF();toggleDownloadMenu() en secuencia,
     pero el listener closeMenu del setTimeout (10ms) ya estaba activo y el
     toggle volvía a abrir el menú al detectar el click.
   - Fix: se agrega closeDownloadMenu() (cierre directo sin toggle) y se llama
     al inicio de generarPDF() y exportarExcel(), antes de cualquier otra lógica.
   - Los botones en modals.html ahora llaman solo generarPDF() y exportarExcel()
     sin el toggleDownloadMenu() redundante.

================================================================================
