================================================================================
================================================================================
==========

INSTRUCCIONES DE VERSIONADO

================================================================================
================================================================================
==========

- Este archivo se SOBRESCRIBE en cada entrega. No se acumulan versiones aquí.

- Cada modificación debe actualizar:

1. index.html  → <meta name="app-version" content="2.MMDD.vXX">

2. index.html  → <p id="appVersionLabel"> con el mismo valor

3. sw.js     → CACHE_NAME = 'pension-utm-vNNN'  (incrementar NNN)

4. Este archivo CAMBIOS.txt con el detalle de qué se modificó

- La letra/sufijo de versión (ej. v55l) debe coincidir siempre con el nombre
del zip entregado (ej. v55l.zip).

================================================================================
================================================================================
==========

>>> AVISO PARA CLAUDE (o cualquier IA que edite este archivo) <<<

Este bloque de instrucciones — desde el separador "====" de arriba hasta el
separador "====" de abajo — es contenido PERMANENTE y NO ES un registro de
cambios editable. NUNCA debe:
  - Borrarse
  - Resumirse
  - Moverse fuera de este archivo
  - Ser sobrescrito al reemplazar el contenido del archivo

Cuando se agregue una nueva entrada de versión (ej. al hacer str_replace o
crear una versión nueva de CAMBIOS.txt), este bloque debe copiarse TAL CUAL
al inicio del archivo nuevo, y luego agregarse la entrada de cambios más
reciente inmediatamente después de él (antes de las entradas anteriores).
Si Claude va a sobrescribir este archivo completo (create_file en vez de
str_replace), DEBE primero leer este bloque del archivo anterior y
copiarlo íntegro al principio del archivo nuevo, sin resumir ni acortar.

Esta instrucción tiene prioridad sobre cualquier instrucción implícita de
"generar un CAMBIOS.txt limpio" o "resumir el historial": el bloque de
instrucciones de versionado NUNCA se resume, se omite ni se trata como
una entrada de changelog más.

>>> FIN AVISO PARA CLAUDE <<<

================================================================================
================================================================================
==========

[v2.0717.0803 · 2026-07-17]
export.js, modals.html

Reemplazado el texto largo de la nota aclaratoria sobre cobertura de
Abonos LAV (dentro del modal "Resumen de liquidación") por una versión
breve, y agregado un botón (i) junto a "Total abonos LAV" que abre un
modal nuevo con la metodología completa de imputación. Sin cambios a
ninguna función de cálculo — es puramente informativo/UI.

1. Nota breve (antes ocupaba un párrafo largo explicando en detalle la
   diferencia entre cobertura aislada vs. resultado final): "La cobertura
   de cada depósito se compara con su mes por separado. El resultado
   final puede variar al sumar todos los depósitos, así que un mes
   'Parcial' o 'Excedente' puede terminar totalmente cubierto."

2. Botón (i) nuevo junto al título "Total abonos LAV" → showLavCoberturaInfo().
   Abre #lavCoberturaInfoModal (nuevo, en modals.html) con: qué es el
   remanente (lo que falta para cubrir la cuota completa del mes), qué es
   el excedente (lo que sobró de la cuota), cómo se imputan los depósitos
   mes a mes (orden cronológico, Art. 1595 CC: primero intereses, luego
   capital, excedente se traspasa automáticamente al siguiente mes con
   saldo pendiente), por qué el estado "Parcial"/"Excedente" de un
   depósito individual no es necesariamente el resultado final, y un
   ejemplo numérico ilustrativo.

3. Nuevas funciones showLavCoberturaInfo()/hideLavCoberturaInfo() en
   export.js — mismo patrón simple de mostrar/ocultar (classList.replace)
   que usan otros modales informativos de la app (ej. ayudaModal).

[v2.0717.0746 · 2026-07-17]
index.html, modals.html, styles.css

Cuatro ajustes de UI a pedido del usuario. Sin cambios a ninguna función
de cálculo.

1. Botón "Ver detalle del cálculo" de la card Total Adeudado: se quitó
   el subtítulo "Igual que el PDF" y el título cambió a "Ver detalle
   mensual", ahora centrado (antes .hero-detail-btn__text usaba
   align-items:baseline con el subtítulo al lado; ahora es un solo
   renglón centrado con justify-content:center).

2. Botón de reinicio ("Reiniciar cálculo") dentro del header del modal
   "Resumen de liquidación" (ícono de flechas circulares, junto al X):
   agrandado de w-7 h-7 (28px) a w-9 h-9 (36px), ícono de w-3.5 h-3.5 a
   w-4 h-4, para mejor visibilidad y área de toque.

3. Tarjetas mensuales (.detail-card--purple): todo el texto pasó a
   blanco puro (#FFFFFF). Antes el monto de interés, su UTM y el UTM del
   capital usaban el acento calipso (#7DE7FF); ahora ese acento se quitó
   de la tarjeta a pedido del usuario y todo el texto es blanco.

4. Título "Total adeudado" de la card principal: tamaño aumentado de
   12px a 15px.

[v2.0717.0729 · 2026-07-17]
styles.css

Fix crítico: las tarjetas mensuales violeta habían quedado con tamaños
de fuente inventados/adivinados (mes 20px, total 34px, etc.) que no
correspondían a las dimensiones reales de la tarjeta original
(.detail-card base: min-height ~78-96px, padding ~0.65-1rem según
breakpoint). Resultado: texto solapado/desbordado (visible en captura
del usuario — "interés" tapado por el monto, números pegados).

Se reescribió .detail-card--purple para IGUALAR los tamaños de texto
de la tarjeta original (antes del rediseño violeta), no adivinarlos:
mes 9px/700 uppercase, total 13px/800, "interés" 8px/700 uppercase,
monto interés 10.5px/700, UTM (ambas) 8px/600 uppercase, badge 8px/700.
Se quitaron border-radius:24px y padding:20px forzados con !important
— ahora la tarjeta hereda el border-radius y padding reales de
.detail-card (que ya varían correctamente por breakpoint), igual que
la tarjeta original. Se removió también el padding-top/border-top
extra que se había agregado a .detail-card__badges (no existía en el
diseño original) y que sumaba altura de más.

Colores (fondo violeta, calipso, blanco) y comportamiento hover se
mantienen sin cambios respecto a v2.0717.0703 — este fix es solo de
dimensionamiento/tamaño de fuente.

[v2.0717.0729 · 2026-07-17]
styles.css

Dos correcciones a pedido del usuario. Sin cambios a ninguna función de
cálculo ni a la estructura HTML de renderDetailsList().

1. FIX IMPORTANTE — tarjetas mensuales (.detail-card--purple) volvieron
   a su tamaño original. El rediseño de v2.0717.0703 había inventado
   tamaños de fuente grandes (mes 20px, total 34px, interés 24px) que
   NUNCA cupieron en la tarjeta real (min-height ~78-96px, la misma de
   antes del rediseño) y producían texto superpuesto/desbordado. Se
   reemplazaron por los tamaños reales que ya tenía .detail-card antes
   de tocar nada: mes 9px, total 13px, "interés" 8px, monto interés
   10.5px, UTM 8px, badge 8px. También se quitaron el border-radius:24px
   y padding:20px que había forzado con !important — ahora la tarjeta
   vuelve a heredar el radius/padding real de .detail-card (y sus
   overrides por breakpoint), en vez de un tamaño adivinado. Solo se
   conserva el color violeta sólido + acento calipso + tipografía Geist.

2. Chip "≈ X UTM" de la card Total Adeudado: estaba muy pegado al monto
   en CLP. Se aumentó el gap de .hero-total-row de 10px a 18px.

[v2.0717.0722 · 2026-07-17]
index.html, styles.css, export.js

Tres ajustes de UI a pedido del usuario. Sin cambios a ninguna función
de cálculo.

1. Chip "≈ X UTM" de la card Total Adeudado: estaba con align-items:
   baseline dentro de .hero-total-row, lo que lo dejaba visualmente más
   abajo que el monto grande (el baseline de un pill con padding no
   coincide con el baseline del texto). Cambiado a align-items:center
   para que quede centrado verticalmente respecto a la altura real del
   monto en CLP, a la derecha, como se pidió.

2. Título "TOTAL ADEUDADO" → "Total adeudado" (minúscula/sentence case)
   en tipografía Geist. Se quitó text-transform:uppercase y letter-spacing
   ancho de .hero-title, y se agregó font-family:'Geist'.

3. Botón "Descargar PDF" del modal "Resumen de liquidación" (al final del
   modal): antes ocupaba el ancho completo (width:100%) con texto+ícono
   alineados a la izquierda. Ahora es un botón compacto (width:auto,
   display:inline-flex, padding 10px 20px) envuelto en un contenedor con
   justify-content:center, en la misma ubicación de siempre pero centrado
   y del tamaño justo para el texto "Descargar PDF" + ícono.

[v2.0717.0715 · 2026-07-17]
styles.css

Fix urgente sobre el rediseño violeta de v2.0717.0703: en pantallas
angostas el monto total ($161.387, $162.033, etc.) se estaba partiendo
en 4-5 líneas dentro de la tarjeta, tapando el resto del contenido.
Causa: .detail-card__total tenía font-size fijo de 34px + word-break:
break-word, que rompía el número letra por letra cuando la columna
(grid de 2 tarjetas por fila) no tenía ancho suficiente.

Fix: font-size de .detail-card__total y .detail-card__int-val ahora usan
clamp() (20-34px y 15-24px respectivamente) en vez de tamaño fijo, y
white-space:nowrap en vez de word-break — el número se achica para
entrar en una sola línea en vez de partirse. También se acotó
.detail-card__int a max-width:44% para que no le quite demasiado
espacio horizontal a la columna del monto principal. Sin cambios de
layout/estructura, solo de dimensionamiento tipográfico.

[v2.0717.0703 · 2026-07-17]
index.html, styles.css

Rediseño visual de las tarjetas mensuales del calendario de pagos
(componente detail-card / renderDetailsList()), solo para meses normales
(no adeudo histórico, que conserva su estilo naranja/gradiente sin cambios).
Sin cambios de layout, tamaño ni contenido — mismo orden de información
(mes → total+UTM → interés+UTM → badge). Sin cambios a ninguna función
de cálculo.

1. Nueva clase .detail-card--purple en styles.css: fondo violeta sólido
   #6844E8 (sin gradiente; ~8% más claro que el morado del botón de año
   activo #5B34E6, para que ambos pertenezcan a la misma familia visual
   sin competir entre sí), border-radius 24px, sin borde, sombra suave
   (0 8px 24px rgba(40,20,120,.18)), hover con translateY(-2px) y sombra
   ligeramente mayor (200ms ease).

2. Tipografía Geist en toda la tarjeta con la jerarquía pedida: mes 20px/700,
   total 34px/800, "interés" 14px/500, monto interés 24px/700, UTM 15px/500,
   badge 12px/600.

3. Colores: texto principal y monto total en blanco puro; etiqueta "interés"
   en blanco 75%; monto interés y UTM del capital en calipso #7DE7FF (único
   acento de la tarjeta); UTM del interés en blanco 80%; línea divisoria
   1px rgba(255,255,255,.12).

4. Badge "+50% ley" / "Tasa máx." rediseñado como pill (border-radius 999px,
   fondo rgba(255,255,255,.12), texto blanco, sin azul) solo para la
   variante violeta; la variante de adeudo histórico (naranja) conserva su
   badge de texto plano sin cambios.

5. renderDetailsList() reestructurado: el HTML de las tarjetas de mes normal
   ahora usa clases semánticas (.detail-card__mes, __row, __cap, __total,
   __utm, __int, __int-label, __int-val, __int-utm, __badges, __badge) en
   vez de utilidades Tailwind inline, para que el nuevo bloque CSS las
   controle. La rama isDebt (adeudo histórico) no se tocó: mismo HTML/
   estilos inline que antes.

[v2.0717.0637 · 2026-07-17]
index.html, modals.html, export.js, styles.css

Tres ajustes de UI a pedido del usuario. Sin cambios a ninguna función de
cálculo.

1. Botón "Descargar" simplificado: eliminado el menú desplegable PDF/Excel.
   Ahora el botón (tanto en el header de "Detalle del cálculo" como en el
   modal "Resumen de liquidación") llama directo a generarPDF(). Exportación
   a Excel desactivada: se quitó el <script> de ExcelJS de index.html y el
   <div id="downloadMenu"> de modals.html; exportarExcel() se dejó definida
   en export.js sin uso (por si se reactiva a futuro) y toggleDownloadMenu()
   se redujo a una función vacía (closeDownloadMenu() sigue siendo llamada
   desde dentro de generarPDF(), así que se conserva como no-op en vez de
   borrarla).

2. Botón de reset ("Limpiar cálculo") agrandado: de 16px de ícono/padding 5px
   sin fondo, a 20px de ícono/padding 8px con fondo rojo claro permanente
   (#fef2f2), para que sea visible sin pasar el mouse por encima y el área
   de toque sea mayor en móvil.

3. Card "Total Adeudado" compactada y el chip "≈ X UTM" movido al lado
   derecho del monto en CLP (antes iba debajo, ocupando una línea propia).
   Nuevo contenedor .hero-total-row (flex, align-items:baseline) envuelve
   #totalGrand y #totalGrandUtm. Padding de .hero-card reducido de
   18px/16px a 14px/12px, márgenes de .hero-date, hr y .hero-row también
   reducidos, y tamaño de fuente de .hero-total bajado de clamp(34-46px) a
   clamp(30-40px) para que quepa junto al chip UTM sin desbordar en pantallas
   angostas.

[v2.0716.0906 · 2026-07-16]
index.html, modals.html, export.js, styles.css

Tres cambios a pedido del usuario sobre el flujo principal y el modal
"Resumen de liquidación". Sin cambios a ninguna función de cálculo.

1. Ocultas las dos barras que quedaban entre la card "Total Adeudado"
   y la sección "¿Cómo deseas calcular?": el panel colapsable "Detalle
   del cálculo" (id calcSummaryCard, con sus 3 íconos de acción) y la
   fila de pills de año + "N meses" (id yearTabsCard). Restaban mucho
   espacio vertical y su información ya es accesible por otra vía (el
   botón morado "Ver detalle del cálculo" de la card Total Adeudado,
   agregado en v2.0716.0905, abre el mismo contenido en el modal). No
   se eliminó el HTML ni la lógica interna de ninguna de las dos —
   renderCalcSummaryCard() sigue actualizando #calcSummaryContent y
   renderYearTabs() sigue generando las pills — solo se quitó el
   classList.remove('hidden')/se forzó classList.add('hidden') para
   que ninguna vuelva a mostrarse, por si se reutilizan en otro lugar
   de la UI más adelante.

2. Botón "Descargar" agregado al FINAL del modal "Resumen de
   liquidación" (buildResumenContent() en export.js, después de la
   nota "Toca cualquier fila..."), con el mismo estilo que el botón
   morado "Ver detalle del cálculo" (gradiente #6F3DFF→#5A2DFF→#4A24E8,
   ícono + texto + chevron) pero sin subtítulo, ya que aquí no aplica
   "Igual que el PDF". El ícono anterior de descarga que vivía en el
   header del panel colapsable (ahora oculto, ver punto 1) sigue
   existiendo ahí sin cambios — este es un botón nuevo y adicional,
   más visible al final del recorrido del resumen. Técnicamente:
   toggleDownloadMenu() se modificó para aceptar un parámetro opcional
   (sourceBtn) en vez de leer siempre document.getElementById
   ('downloadBtn') — así el nuevo botón (id downloadBtnResumen) puede
   posicionar el mismo menú #downloadMenu sin duplicar el id
   "downloadBtn" en el DOM. La llamada original sin argumento
   (onclick="toggleDownloadMenu()" del botón del panel colapsable)
   sigue funcionando exactamente igual que antes.

3. Botón de "Eliminar / Limpiar cálculo" (antes ícono de basurero rojo
   en el header del panel colapsable, ahora oculto) reemplazado por un
   botón de "Reiniciar" (ícono de flechas circulares/refresh) ubicado
   en el header del modal "Resumen de liquidación" (modals.html), a la
   izquierda del botón de cerrar (X). Llama a la misma función
   showResetConfirm() de siempre (sin cambio de comportamiento del
   flujo de confirmación en sí). Cambios de soporte:
   - showResetConfirm()/hideResetConfirm() (index.html): se agregó
     manejo de z-index para que confirmResetModal (z-index base 110)
     quede por encima de resumenModal (z-index 120) cuando se abre
     desde dentro de él — mismo patrón que ya usa showResumenModal()
     para subirse por encima de dupOverlay/befOverlay.
   - resetAll(): si el modal de resumen estaba abierto al confirmar el
     reset, ahora se cierra automáticamente al final (hideResumenModal())
     para no dejar datos obsoletos visibles tras el reset.

4. Grid "¿Cómo deseas calcular?": revertido el recorte de v2.0716.0905
   — la descripción de cada tarjeta (ej. "Calcula desde tu último pago
   registrado") vuelve a mostrarse, pero ahora en texto pequeño (10px,
   gris #8e97b0, máximo 2 líneas con line-clamp) en vez de ocultarse
   por completo. Las tarjetas crecieron ligeramente (min-height 76px
   → 92px, padding 12px → 14px, ícono 34px → 38px) para acomodar la
   descripción sin volver al tamaño excesivo de v2.0716.0904.

Versión: index.html (meta app-version + appVersionLabel) y sw.js
(CACHE_NAME) actualizados a v2.0716.0906.

No se tocó: ninguna función de cálculo, los overlays DUP/BEF, ni el
generador de PDF/Excel (generarPDF/exportarExcel).

--------------------------------------------------------------------------------

[v2.0716.0905 · 2026-07-16]
index.html, styles.css

Fix de tamaño — el rediseño anterior (v2.0716.0904) quedó desproporcionado
en el dispositivo real: la card "Ver detalle del cálculo" ocupaba
mucho más espacio del previsto (el diseño no consideraba que título +
subtítulo podían quebrar en 2-3 líneas en pantallas angostas) y el
grid "¿Cómo deseas calcular?" quedó demasiado alto por las
descripciones de 2 líneas bajo cada título.

1. Eliminado el banner verde "Actualizado hace X" (.hero-status,
   id="heroUpdatedAgo") de la tarjeta "Total Adeudado" — a pedido
   expreso del usuario. En su lugar, en el MISMO espacio y tamaño que
   ocupaba ese banner (padding 10px 14px, border-radius 12px, dentro
   de la card hero existente), ahora va el botón "Ver detalle del
   cálculo" (.hero-detail-btn, nuevo): franja compacta de una sola
   línea con gradiente violeta #6F3DFF→#5A2DFF→#4A24E8, ícono de
   documento (16px), título+subtítulo en la misma línea (11.5px /
   10.5px, con overflow:ellipsis si no cabe) y chevron a la derecha.
   Conectado a showResumenModal() (mismo comportamiento que ya tenía
   el botón "Ver PDF" del panel colapsable "Detalle del cálculo",
   que sigue existiendo sin cambios más abajo).

2. El id="heroUpdatedAgo" se conservó oculto (span vacío dentro del
   bloque de "IDs conservados ocultos" que ya existía en el hero) para
   no romper heroTickUpdatedLabel()/heroMarkUpdated() ni el
   setInterval(heroTickUpdatedLabel, 30000) — esa función ya tenía
   null-check (if (!el ...) return;), así que aunque el span esté
   oculto no genera error; simplemente deja de mostrarse.

3. Grid "¿Cómo deseas calcular?" (.calc-mode-card, agrandado en
   v2.0716.0904 a min-height 120px con descripción de 2 líneas):
   revertido a un formato compacto y rectangular — min-height 76px,
   border-radius 16px, padding 12px, ícono 34px (antes 56px), solo
   ícono + título (se eliminó el <p class="calc-mode-card__desc"> de
   las 4 tarjetas y su regla CSS asociada, ya que no cabía sin
   agrandar demasiado la tarjeta). El chevron de cada tarjeta se achicó
   de 20px a 15px y el título de sección "¿Cómo deseas calcular?" bajó
   de 22px a 16px con menos margen inferior (20px → 12px).

4. Limpieza: se eliminó el CSS huérfano .calc-hero-card* (~9 reglas)
   que había quedado sin uso en el HTML tras el cambio anterior — esa
   card ya no vive como bloque aparte sobre el grid, ahora es
   .hero-detail-btn dentro de la card "Total Adeudado".

Versión: index.html (meta app-version + appVersionLabel) y sw.js
(CACHE_NAME) actualizados a v2.0716.0905.

No se tocó: ninguna función de cálculo, el panel colapsable "Detalle
del cálculo" con sus botones de PDF/descargar/eliminar (sigue
intacto más abajo en el flujo), ni los overlays fullscreen DUP/BEF.

--------------------------------------------------------------------------------

[v2.0716.0904 · 2026-07-16]
index.html, styles.css

Rediseño de la sección "¿Cómo deseas calcular?" (index.html, dentro de
la sección <section class="calc-mode-section">, justo antes de la
tarjeta "Datos de la Pensión") a partir de un brief de UX/UI provisto
por el usuario (mockup + prompt tipo Claude Code, estilo iOS moderno,
inspirado en Revolut/Nubank/Linear). Cambio ÚNICAMENTE visual —
ninguna función JS, ID de estado, ni lógica de negocio fue modificada;
los 4 botones del grid siguen sin acción funcional propia (ya lo
indicaba el comentario original en el HTML), salvo la nueva card hero
que sí quedó conectada.

1. Nueva card "Ver detalle del cálculo" (.calc-hero-card, 100% nueva):
   no existía en el proyecto — el único componente similar era el
   panel colapsable "Detalle del cálculo" (violeta claro, más chico),
   que es un elemento distinto y no se tocó. La card nueva es un botón
   ancho con gradiente horizontal #6F3DFF → #5A2DFF → #4A24E8,
   border-radius 24px, ícono de documento en outline blanco (34px),
   título "Ver detalle del cálculo" (22px/600/blanco) y subtítulo
   "Revisa el cálculo completo igual que el PDF" (16px/blanco al 75%
   de opacidad), con chevron derecho blanco (28px). Al tocarla invoca
   showResumenModal() (la misma función que ya usaba el botón "Ver PDF"
   del panel colapsable), por lo que queda funcional desde el primer
   momento sin necesidad de lógica nueva.

2. Grid "¿Cómo deseas calcular?" (.calc-mode-card): agrandado de 84px
   a min-height 120px, con padding 18px y border-radius 22px (antes
   16px). Se agregó texto de descripción de 2 líneas bajo cada título
   (antes solo había título) — "Calcula desde tu último pago
   registrado", "Calcula entre dos fechas específicas", "Considera
   períodos sin pago (lagunas)", "Registra abonos parciales y su
   imputación" — y un chevron individual en la esquina superior
   derecha de cada tarjeta, del mismo color que su ícono (antes las
   flechas estaban ocultas con display:none en el formato compacto).

3. Iconos del grid: agrandados de 32px a 56px (radio 16px), ahora con
   fondo sólido en vez del gradiente diagonal anterior — azul #1678FF,
   morado #6F3DFF, verde #10C98D, fucsia #F23BAE — según la paleta
   exacta del brief.

4. Contenedor de la sección: se quitó el fondo blanco + borde + sombra
   que envolvía todo el bloque como una sola tarjeta grande (ya no
   aplica: ahora la card hero y el grid son bloques visualmente
   independientes sobre el fondo de la app), y el título "¿Cómo deseas
   calcular?" pasó de 10px/uppercase/gris a 22px/700/#2E3150 en
   sentence case, con 32px de espacio respecto a la card hero de
   arriba y 20px de margen inferior antes del grid.

5. Espaciado del grid: gap de 10px → 16px (horizontal y vertical),
   como pedía el brief.

Versión: index.html (meta app-version + appVersionLabel) y sw.js
(CACHE_NAME) actualizados a v2.0716.0904.

No se tocó: el panel colapsable "Detalle del cálculo" (con sus botones
de PDF/descargar/eliminar), ningún overlay fullscreen (DUP "Desde el
último pago", BEF "Entre fechas"), ni ninguna función de cálculo.
Los 3 botones del grid sin handler ("Entre fechas", "Con lagunas",
"Pagos parciales") mantienen el mismo comportamiento de antes (ninguno
— eran decorativos ya en la versión previa); si se desea conectarlos a
sus overlays/flujos correspondientes, es un cambio aparte de agregar
handlers, no de este rediseño visual.

Pendiente: confirmar en el dispositivo real que las 4 tarjetas del
grid, ahora más altas (min-height 120px) con texto de descripción,
no generan overflow o descuadre en pantallas muy angostas (<340px de
ancho), y que la card hero morada se ve correctamente por encima del
notch/safe-area en iOS.

--------------------------------------------------------------------------------

[v2.0716.0903 · 2026-07-16]
export.js, modals.html

Rediseño visual del modal "Resumen de liquidación" (buildResumenContent
en export.js + header en modals.html), a partir de un brief de UX/UI
provisto por el usuario inspirado en Vercel/Linear/Stripe Dashboard/
Notion/Arc/Revolut. Cambio ÚNICAMENTE visual (colores, tipografía,
espaciado, forma de los componentes) — NO se modificó ninguna variable,
ID, función de cálculo, ni el orden/estructura de los datos mostrados.

1. Header del modal (modals.html): quitado el uppercase del título
   ("Resumen de liquidación" en vez de "RESUMEN DE LIQUIDACIÓN"), fondo
   #FAFAFC, ícono más pequeño (7×7 en vez de 8×8) en violeta #EEF2FF/
   #4F46E5, padding vertical reducido (py-3 en vez de py-4) para bajar
   la altura del header.

2. Tabla de período (tablaFilas): header de columnas ahora con fondo
   #FAFAFC y texto #667085 (antes #9095A1 sin fondo). Colores de columna
   alineados a la paleta del brief: UTM #4F46E5, Capital #111827, Días
   #6B7280, Interés #0EA5E9, Subtotal #111827 en negrita. La Tasa ahora
   se muestra dentro de una píldora (fondo #EFF6FF, texto #2563EB,
   border-radius:999px) en vez de texto plano celeste.

3. Separador de año: convertido en badge/píldora (fondo #EEF2FF, texto
   #4F46E5, padding 6px 12px, border-radius 999px) en vez de solo texto
   violeta sin fondo — ahora se distingue claramente como separador
   visual, tal como pedía el brief ("cada año debe ser un pequeño
   badge").

4. Bordes: unificados globalmente en export.js de #ECEFF5 → #ECECF3 y
   de #F3F4F7 → #F3F4F6 (los valores exactos del brief; visualmente
   casi idénticos a los anteriores, así que el cambio no afecta ningún
   otro flujo — Abonos Realizados, Pagos Parciales, tabla de reajustes,
   etc. también heredan el tono correcto).

5. Sección "Abonos LAV": rediseño completo de la presentación de cada
   depósito. Ya no es una fila con separador de línea — ahora cada
   depósito es una tarjeta individual (fondo blanco, borde 1px solid
   #EEF0F4, border-radius 14px, padding 16px, con margen entre
   tarjetas), dentro de un contenedor #FAFAFC con borde #ECECF3 y radio
   16px. Se eliminó el ícono/label verde saturado (#1a9c56) del label
   "Depósitos", ahora en violeta #4F46E5 sobre fondo transparente. El
   texto "Rem." (naranja) del indicador de cobertura parcial se
   reemplazó por "Parcial:" en índigo #4338CA; el indicador de
   excedente ahora usa verde #059669 en vez del verde institucional
   anterior.

6. Tarjeta "Total abonos LAV": rediseñada como tarjeta simple
   izquierda/derecha (antes era una franja de fondo verde con 3
   columnas). Izquierda: "Total abonos LAV" en #111827. Derecha: monto
   CLP grande (700, #111827) arriba y UTM debajo (500, #667085) — según
   el brief, "la UTM debe ir debajo del monto, no al lado".

7. Nota inferior de cobertura LAV: convertida en caja informativa
   (fondo #F8FAFC, borde #EEF2F6, border-radius 14px) con ícono de
   información azul (#2563EB) a la izquierda y texto en #667085 a 11px
   — antes era un párrafo en cursiva sin caja ni ícono.

8. Títulos de sección (función seccion(), usada por "Período actual",
   "Deuda histórica", "Abonos realizados", "Pagos parciales por mes",
   "Abonos LAV", "Resumen final"): eliminado el uppercase y el
   letter-spacing; ahora usan sentence case, font-weight 700, color
   #111827 — igual que pedía el brief para todos los títulos de la
   interfaz. Los textos pasados a seccion() en las llamadas del modal
   web también se normalizaron a sentence case (ej. "DEUDA HISTORICA
   CONSOLIDADA" solo se cambió en el modal web; el bloque del generador
   de PDF —líneas ~150-400 de export.js— no se tocó, ya que ese texto
   alimenta el documento PDF, no la interfaz).

9. Filas del bloque "Resumen final": colores de "Capital total" e
   "Total intereses generados" alineados a #111827 / #0EA5E9; el label
   "SUBTOTAL HISTÓRICO" pasó a "Subtotal histórico" en sentence case.

Versión: index.html (meta app-version + appVersionLabel) y sw.js
(CACHE_NAME) actualizados a v2.0716.0903.

No se tocó: ninguna función de cálculo (calcularInteresSegmentado,
calcularInteresConsolidadaNeteada, imputarAbonosArt1595, el pool LAV),
ningún ID usado por JS para leer/escribir datos, el generador de PDF
(generarPDF() y sus helpers en la primera mitad de export.js), ni el
orden/estructura de las secciones del resumen.

Pendiente: confirmar visualmente en el dispositivo real que el modal
completo (con datos reales de un caso con Deuda Histórica Consolidada +
Abonos LAV) refleja el diseño esperado, especialmente el espaciado de
las nuevas tarjetas de depósito LAV en pantallas angostas.

--------------------------------------------------------------------------------

[v2.0716.0902 · 2026-07-16]
index.html, styles.css

Rediseño completo de la tarjeta "Total Adeudado" a partir de una
referencia HTML/CSS provista por el usuario (screenshot + código): ya
NO reutiliza el layout de .dup-summary-card del overlay DUP (eso era
el diseño de la iteración anterior, v2.0716.0901); ahora tiene su
propia clase .hero-card con estructura y estilos propios.

Estructura nueva: título pequeño "TOTAL ADEUDADO" en violeta
(uppercase, letter-spacing) arriba a la izquierda, con la fecha de
liquidación debajo en gris ("Al 16 Jul 2026"); botón hamburguesa
arriba a la derecha (ahora SVG de 3 líneas en vez del ícono circular
de $ anterior, más fiel a la referencia — no se usó Font Awesome
porque no está cargado en el proyecto). Debajo: monto grande en
violeta (#5B43F4), chip UTM con fondo lila claro, línea divisoria,
filas Capital / Intereses acumulados, otra línea divisoria, y una
barra de estado verde "Actualizado hace X" con ícono de check.

1. index.html (markup hero, ~línea 1250): estructura
   .hero-card > .hero-header (.hero-title + .hero-date / .hero-menu),
   .hero-total, .hero-utm, <hr>, dos .hero-row (Capital/Intereses),
   <hr>, .hero-status. IDs reutilizados sin cambio de significado:
   totalGrand, totalGrandUtm, totalCap, totalInt. heroFechaLiq pasó de
   ser el título completo ("Total adeudado al...") a ser solo la
   fecha bajo el título ("Al 16 Jul 2026"), ya que el título fijo
   "TOTAL ADEUDADO" ahora vive en el HTML estático (.hero-title). Se
   agregó el id heroUpdatedAgo (span dentro de .hero-status) para el
   texto "Actualizado hace X minutos". Los ocultos totalCapUtm/
   totalIntUtm/heroMetaRow/heroUtmRef/heroParcialRow/heroLavRow se
   mantienen igual que en la versión anterior (siguen recibiendo
   valores sin guard null-check desde el JS de cálculo).

2. index.html (JS): los dos bloques que escriben heroFechaLiq (dentro
   de calculate(), rama con datos y rama de reset a $0) cambiaron el
   texto de "Total adeudado al DD Mmm AAAA" a "Al DD Mmm AAAA". Se
   agregó heroMarkUpdated() / heroTickUpdatedLabel() (definidas junto
   a getFechaLiquidacion()): heroMarkUpdated() guarda
   Date.now() en _heroLastUpdate y refresca el label de inmediato;
   heroTickUpdatedLabel() calcula el texto relativo ("hace un
   momento" / "hace N minutos" / "hace N horas" / "hace N días") a
   partir de ese timestamp. Un setInterval cada 30s mantiene el label
   actualizado sin necesidad de un nuevo cálculo. heroMarkUpdated() se
   invoca al final de ambas ramas de calculate() (con datos y con
   reset a $0) para que el estado "Actualizado hace..." refleje el
   momento del cálculo real, no un texto fijo como en la referencia
   original del usuario.

3. styles.css: se agregó el bloque .hero-card / .hero-header /
   .hero-title / .hero-date / .hero-menu / .hero-total / .hero-utm /
   .hero-row / .hero-status (y el selector .hero-card hr) justo
   después de .hero-cards-row. Los tamaños de la referencia del
   usuario (64px el monto, 26px de padding, etc.) estaban pensados
   para escritorio; aquí se redujeron y el monto usa
   clamp(34px, 11vw, 46px) para que se ajuste a una pantalla de
   celular sin desbordar ni necesitar font-fit dinámico en JS. Las
   reglas .hero-total-card / .dup-summary-* siguen existiendo en el
   archivo (ya sin uso en el hero, pero .dup-summary-* SÍ sigue
   siendo usada por el overlay "Desde el último pago" — no tocar).

--------------------------------------------------------------------------------

[v2.0716.0901 · 2026-07-16]
index.html, styles.css

Cambio estético — la tarjeta hero "Total Adeudado" de la pantalla
principal ahora reutiliza EXACTAMENTE el layout de la tarjeta de resumen
del overlay "Desde el último pago" (.dup-summary-card): ícono circular
violeta a la izquierda + título/monto/UTM a la derecha, línea divisoria,
y filas "Capital" / "Intereses acumulados" debajo — sin el badge verde
"Calculado hasta hoy" (removido a pedido del usuario). El título ya no
dice "Total adeudado al día de hoy" sino que usa la fecha de liquidación
real, formateada como "Total adeudado al 08 Jul 2026".

1. index.html (markup hero, ~línea 1250): el div .hero-total-card ahora
   también lleva la clase .dup-summary-card y su contenido interno se
   reemplazó por la estructura .dup-summary-header (ícono + content) +
   .dup-summary-divider + dos .dup-summary-row (Capital / Intereses
   acumulados) — mismo HTML que ya usa dupSummarySection en el overlay.
   Los antiguos elementos .hero-total-card__badge (badge verde),
   __icon/__label/__amount-row/__sign/__amount/__utm/__meta fueron
   removidos del DOM (las clases CSS correspondientes quedan sin uso,
   no se borraron del styles.css por si se reutilizan). Los IDs
   totalGrand/totalGrandUtm/totalCap/totalInt se conservan pero ahora
   son los propios elementos .dup-summary-total/.dup-summary-utm/
   strong dentro de .dup-summary-row (antes eran hijos de un layout
   centrado distinto). totalCapUtm/totalIntUtm/heroMetaRow/heroUtmRef
   se mantienen ocultos en el DOM (siguen recibiendo valores sin
   guard null-check desde el JS de cálculo). heroFechaLiq ahora es el
   <span class="dup-summary-title"> que muestra "Total adeudado al
   DD Mmm AAAA" en vez de una línea de meta separada. El botón
   hamburguesa se movió de la esquina superior izquierda a la superior
   derecha para no superponerse con el nuevo ícono circular que ahora
   ocupa esa esquina.

2. index.html (JS dentro de calculate(), bloque que escribe
   totalGrand/totalCap/totalInt/totalGrandUtm/heroFechaLiq): el monto
   grande ahora se escribe con format() (incluye el signo "$" y
   separador de miles) en vez de escribir solo el número y depender de
   un <span> "$" hermano — ya no existe ese span en el nuevo layout, así
   que se eliminó la función fitHeroAmount() (ajuste dinámico de
   font-size) que dependía de grandEl.previousElementSibling; el tamaño
   de fuente ahora lo controla el CSS fijo de .dup-summary-total (30px),
   igual que en el overlay. totalGrandUtm pasó de "23.85000 UTM" a
   "≈ 23,85 UTM" (mismo formato que dupSummaryUtm/befSummaryUtm). El
   bloque que antes armaba heroMetaRow ("UTM ref: ... / Fecha liq: ...")
   se reemplazó por un bloque que arma el string
   "Total adeudado al DD Mmm AAAA" a partir de getFechaLiquidacion() y
   lo asigna a #heroFechaLiq. El mismo bloque de fecha se replicó en la
   rama "reset a cero" (sin período/cálculo activo) para que el título
   siga mostrando la fecha en vez de quedar vacío.

3. index.html (renderLavSummary(), rama "sin cálculo de pensiones"):
   ajustada para escribir grandEl con format() (con "$") y
   grandUtm con el formato "≈ X,XX UTM" en vez del formato viejo
   "X.XXXXX UTM", consistente con el resto de la tarjeta.

4. FIX colateral: dupPopulateSummary() y befPopulateSummary() (overlays
   "Desde el último pago" y "Basado en cuota") concatenaban
   '$' + grandEl.innerText para armar dupSummaryTotal/befSummaryTotal.
   Como ahora grandEl (#totalGrand) YA incluye el signo "$" en su
   innerText, esa concatenación habría producido "$$1.234.567" en
   ambos overlays. Corregido para usar grandEl.innerText directamente
   sin prefijo adicional.

5. styles.css: la regla base .hero-total-card se redujo a solo
   flex/position (ya no define background, border, padding, layout
   flex centrado ni min-height) para que .dup-summary-card sea la
   única fuente de esos estilos y no haya conflicto de cascada al
   combinar ambas clases en el mismo div. Se eliminaron los overrides
   responsive redundantes que forzaban padding/min-height propios de
   .hero-total-card en mobile (@media ancho reducido) y tablet
   (@media min-width:768px), ya que .dup-summary-card ya es responsive
   por sí sola (es la misma tarjeta que usa el overlay DUP en todos los
   tamaños de pantalla). Las clases .hero-total-card__* (badge, icon,
   label, amount-row, sign, amount, utm, meta, lav-pill) NO se
   eliminaron del archivo — quedan como CSS sin uso por si se
   reutilizan en otro lugar más adelante.

--------------------------------------------------------------------------------

[v2.0716.0900 · 2026-07-16]
index.html, styles.css, export.js

Cambio estético — la tarjeta hero "Total Adeudado" y la tarjeta "Detalle
del cálculo" de la pantalla principal adoptan la misma paleta visual que
ya usaba el overlay "Desde el último pago" (fondo blanco, badge verde
"Calculado hasta hoy", ícono circular violeta, tipografía DM Sans,
acentos violeta/celeste) en vez del degradado azul oscuro que tenían
antes. Cambio puramente visual — ningún ID, cálculo, ni estructura de
datos fue tocado; todos los elementos siguen siendo alimentados por el
mismo JS de siempre.

1. HERO "Total Adeudado" (index.html + styles.css, clases
.hero-total-card*): pasó de degradado azul con texto blanco a card
blanco con borde/sombra suave violeta (mismo criterio que
.dup-summary-card), badge verde "Calculado hasta hoy" nuevo, ícono
circular violeta (mismo símbolo "$" del DUP) y el monto grande ahora en
violeta (#5b4fff) en vez de blanco. El botón hamburguesa cambió de
líneas blancas a oscuras (ahora el fondo es claro). Se agregó
.hero-total-card::after con el mismo halo radial sutil que tiene
.dup-summary-card::after. La píldora "Abonos LAV" (oculta por defecto,
se conserva en el DOM) también se adaptó a fondo claro por consistencia,
aunque no está visible actualmente.

2. "DETALLE DEL CÁLCULO" (index.html): el contenedor exterior y su
header colapsable pasaron de acento azul (#3b82f6) a violeta (#5b4fff),
con bordes grises finos (#ececf5) y la misma sombra que el DUP en vez
del borde/sombra genérica anterior.

3. buildResumenContent() en export.js — usada tanto por esta tarjeta
inline como por el modal "Resumen de Liquidación" y el PDF comparte
código, así que el repintado benefició a ambos lugares:
   a. FIX: la función seccion() (label de cada bloque: "Período Actual",
      "Deuda Histórica", "Abonos Realizados", etc.) tenía
      "if (inlineMode) return" — es decir, en la tarjeta de la página
      principal NUNCA se mostraba ningún label de sección. Por eso,
      como notó el usuario, la tabla de "Deuda Histórica" aparecía sin
      ningún texto que la identificara. Ahora el label se muestra en
      ambos modos, con un estilo limpio (texto uppercase + línea
      divisoria fina gris) en vez de la franja de fondo tintada de
      color por sección que tenía el modal antes.
   b. Se reemplazaron los bordes/fondos tintados por color de cada
      sección (azul para pensiones, naranja para deuda histórica,
      celeste para abonos, violeta para pagos parciales, verde para
      LAV) por un borde gris fino uniforme (#ECEFF5) en todas las
      tablas y bloques — mismo criterio visual que usa el DUP. Los
      acentos de color con significado semántico propio (violeta para
      columna UTM, celeste para tasa/interés, verde para LAV, naranja
      para marca "H" de histórico, violeta/verde para chips de
      remanente/excedente) SE MANTUVIERON — no se tocó ningún dato ni
      chip informativo, solo el color de fondo/borde de los
      contenedores.
   c. El bloque "Resumen final" (header + filas + total destacado)
      cambió su ícono de verde a violeta y el total final destacado
      (solo visible en el modal, no en la tarjeta inline) pasó del
      degradado azul oscuro con texto blanco a fondo violeta claro
      (#f5f6ff) con el monto en violeta (#5b4fff) — mismo lenguaje que
      el resto.
   d. El bloque colapsable "Datos del Caso" (solo modal) también
      adoptó la paleta violeta/gris.

Alcance: NO se reestructuraron las tablas (se mantuvo la columna UTM
visible, separadores por año, chips de remanente/excedente y el
click-to-detail de cada fila) ni se movió información a otra vista —
a pedido explícito del usuario, este cambio es solo de "limpieza
visual" (menos bordes/recuadros de colores distintos por sección,
labels de sección donde faltaban) sobre la estructura de datos
existente, no un rediseño.

================================================================================
================================================================================
==========

[v2.0716.0800 · 2026-07-16]
index.html

Fix — botón "atrás" del sistema en Android cerraba la app completa (en vez
de solo el overlay abierto) al presionarlo dentro de los overlays fullscreen
"Entre fechas", "Desde el último pago" y "Conversor UTM y pesos". Causa: al
ser overlays superpuestos (display:flex + z-index), no páginas reales, no
empujaban ninguna entrada al historial del navegador — el botón de retroceso
del sistema no encontraba historial dentro de la PWA y salía directo de la
aplicación.

Se agregó un gestor de historial (_histPush / _histPop, bloque nuevo
"Gestor de historial para overlays fullscreen" cerca del inicio del
archivo): cada open<X>Overlay() ahora empuja una entrada ficticia al
historial vía history.pushState(); un listener de 'popstate' detecta
cuándo el usuario presiona atrás con alguno de estos tres overlays abierto
y lo cierra visualmente en vez de dejar que el navegador salga de la app.

Para evitar un loop entre el cierre manual (botón "X" del overlay, que
llama a history.back() para limpiar la entrada) y el cierre por popstate
(que NO debe volver a llamar a history.back()), se separó en cada overlay
la función de cierre en dos: closeXOverlay() (cierre manual, consume la
entrada de historial) y _closeXOverlayVisual() (solo oculta el DOM,
reutilizada por el handler de popstate). El comportamiento visual del
cierre (animación, reset de formulario, etc.) es idéntico al de antes —
no se tocó ninguna lógica de cálculo ni de UI dentro de los overlays.

Alcance de este fix: los tres overlays fullscreen mencionados (dup, bef,
conv). Otros modales de la app (detalle de período, casos, ayuda, etc.)
no fueron tocados — quedan pendientes si se reporta el mismo problema en
ellos.

================================================================================
================================================================================
==========

[v2.0716.0700 · 2026-07-16]
export.js

Fix + Mejora — columna "Estado período" agregada en v2.0716.0600 a la
tabla ABONOS LAV del PDF.

FIX: faltaba el monto en pesos equivalente — solo mostraba el UTM
("Parcial · Rem. 0.101 UTM"). Ahora muestra ambos: "Parcial · Rem.
0.101 UTM ($6.800)" / "Excedente 0.048 UTM ($3.400)", igual que ya
hacía la tarjeta "Depósitos LAV" y el Resumen web.

ACLARACIÓN IMPORTANTE (a pedido del usuario, tras revisar el PDF
generado): esta columna compara cada depósito de forma AISLADA contra
la cuota de su mes reasignado — no es el resultado final por período.
Como el pool de depósitos LAV es acumulado (el excedente de un mes se
traspasa y cubre el faltante de otro), casi todos los meses aparecen
"Parcial" o "Excedente" en esta columna aunque terminen 100% cubiertos
en el resultado real (el único remanente real del caso de prueba
"Test1" es Sep-2025, según la liquidación oficial PJUD).

Se evaluó corregir el criterio para que reflejara el estado acumulado
real, pero se optó por MANTENER la comparación aislada (útil para ver
la cobertura de cada depósito individual) y en su lugar agregar una
nota aclaratoria justo debajo de la tabla, tanto en el PDF como en el
Resumen de Liquidación web, explicando que el resultado definitivo por
período está en la tabla "Pensiones Mensuales Impagas" (PDF) / el
detalle por período (Resumen web), no en esta columna.

================================================================================
================================================================================
==========

[v2.0716.0600 · 2026-07-16]
index.html, export.js

Nueva función — Info de "Parcial · Rem." (cobertura/remanente) de cada
depósito LAV agregada al Resumen de Liquidación (vista web) y al PDF.
Hasta ahora ese dato solo existía en la tarjeta "Depósitos LAV" de la
pantalla principal; en el Resumen y el PDF cada depósito solo mostraba
fecha, monto y equivalencia UTM, sin indicar si ese mes quedó cubierto,
parcial o con excedente.

REFACTOR (index.html): se extrajo la lógica de cálculo de cobertura que
ya vivía duplicada dentro de renderItem() de renderAbonosLav() a una
función pura reutilizable, calcCoberturaLavDeposito(p). Recibe un abono
de abonosLav y devuelve { estado: 'parcial'|'excedente'|'cubierto'|null,
diffUTM, remanenteClp, excedenteClp, coberturaPct }, calculado sobre el
período (p.periodo) al que quedó reasignado ese depósito, igual que
antes. renderItem() ahora llama a esta función en vez de tener el
cálculo duplicado — mismo comportamiento, cero cambios visuales en la
tarjeta original.

NUEVO (export.js):
- buildResumenContent(): cada fila de depósito LAV ahora agrega, cuando
  corresponde, una línea "⚠ Parcial · Rem. X UTM ≈ $Y" (ámbar) o "↪
  Excedente X UTM ≈ $Y" (verde) debajo de fecha/monto. Si el mes queda
  cubierto exacto, no se agrega texto extra — mismo criterio visual que
  la tarjeta "Depósitos LAV".
- generarPDF(): la tabla "ABONOS LAV" ganó una séptima columna "Estado
  período" con el mismo dato ("Parcial · Rem. X UTM" / "Excedente X
  UTM" / "Cubierto"), calculado con la misma función.

Los tres lugares (tarjeta, Resumen, PDF) comparten ahora una única
fuente de verdad para este cálculo — evita que diverjan entre sí en el
futuro.

================================================================================
================================================================================
==========

[v2.0716.0500 · 2026-07-16]
export.js

Nueva función — Edición manual inline en la vista previa de importación
OCR de Abonos LAV (_ocrRenderPreview). Contexto: el motor de OCR (Groq)
a veces lee mal un dígito de un monto (ej. $155.000 → $150.000,
confusión visual 5↔0), y aunque la validación fila-por-fila agregada en
v2.0715.2226 detecta la mayoría de estos casos marcándolos "⚠️
Revisar", hasta ahora la única forma de corregirlos era borrar la fila
y volver a ingresarla a mano en otro flujo distinto.

Se agregó un botón "✎" junto al de borrar (✕) en cada fila de la vista
previa. Al tocarlo, la fila se reemplaza por un mini-formulario inline
con fecha, monto en pesos y sección (LAV / Otros Abonos). Al guardar:
- El monto en UTM se recalcula automáticamente desde el monto en pesos
  corregido y la UTM del mes de la fecha ingresada (misma fuente
  utmData que usa el resto de la app) — el usuario nunca escribe el
  valor UTM a mano, evitando errores de digitación ahí.
- La fila deja de estar marcada "⚠️ Revisar" automáticamente (al
  recalcularse monto_utm desde monto_pesos, ya no puede haber
  inconsistencia interna entre ambos).
- Queda logueada en el panel de debug: fecha, monto y sección final
  tras la corrección.

_ocrRemoveItem() ahora también reajusta/limpia el índice de edición
activa (_ocrEditandoIndex) si se borra la fila que estaba siendo
editada, para evitar que el formulario quede huérfano tras el borrado.

Nueva variable de módulo _ocrEditandoIndex (null = ninguna fila en
edición), reseteada en ocrReset().

Compatibilidad: ocrConfirmar() no requirió cambios — ya consumía
item.fecha / item.monto_pesos / item.monto_utm / item.seccion en el
mismo formato que produce la edición manual.

================================================================================
================================================================================
==========

[v2.0715.2226 · 2026-07-15]
export.js

Mejora — Blindaje adicional al importador OCR de Abonos LAV (runOcrAnalysis
/ _ocrRenderPreview). Se detectó que, al procesar DOS VECES el mismo
documento PJUD (caso "Test1", RIT Z-617-2024), el modelo de OCR (Groq/
OpenRouter) leyó de forma distinta el monto en pesos de la fila del
depósito 02-08-2024: $155.000 en una corrida, $150.000 en otra. La
validación de suma total ya existente (comparar la suma de filas contra
el total impreso del documento) no siempre detecta esto, porque si el
modelo también reporta un total "coherente" con su propia lectura
errónea, la suma cuadra igual aunque esté mal frente al documento real.

FIX: nueva validación cruzada FILA POR FILA. Para cada movimiento que
trae tanto monto_pesos como monto_utm, se recalcula monto_pesos ÷
UTM-del-mes-de-la-fila y se compara contra el monto_utm que el propio
modelo reportó para esa misma fila (tolerancia 1,5%, por posibles
diferencias de redondeo del valor UTM impreso vs. tabla SII de la app).
Si no coinciden, es señal de que el modelo se contradijo a sí mismo al
leer esa fila — se marca como sospechosa.

Efectos:
- Se agrega un confirm() de advertencia adicional (independiente del ya
  existente por discrepancia de suma total) listando las filas
  sospechosas antes de pasar a la vista previa.
- En la vista previa (_ocrRenderPreview), las filas marcadas se pintan
  con borde/fondo rojo y un badge "⚠️ Revisar" junto al monto.
- _ocrRemoveItem() reajusta los índices sospechosos al borrar una fila,
  para que el badge no quede desalineado.
- Nueva variable de módulo _ocrFilasSospechosas, reseteada en ocrReset().

LIMITACIÓN CONOCIDA (documentada en el código): si el modelo lee mal UN
monto y deriva el otro de esa misma lectura errónea (ambos "coherentes"
entre sí pero incorrectos frente al documento), esta validación no lo
detecta — solo atrapa contradicciones internas del propio modelo. Sigue
siendo responsabilidad del usuario revisar visualmente cada fila contra
el documento original antes de confirmar la importación.

================================================================================
================================================================================
==========

[v2.0715.2225 · 2026-07-15]
export.js

Fix visual — PDF de liquidación (generarPDF): se eliminó la línea
duplicada "Pension UTM Pro vX.XX.XXXX" del ENCABEZADO (aparecía centrada
debajo de "Generado: ... | UTM ref: ..."). El usuario reportó que el
total ("TOTAL A PAGAR — LIQUIDACION FINAL") quedaba muy abajo en la
página 2 tras agregar esa línea en una versión anterior.

La versión de la app ya se muestra, una vez por página, en el PIE de
página junto al disclaimer legal ("Pension UTM Pro – Calculo
referencial · vX.XX.XXXX", esquina inferior izquierda, junto a "Pag. X
/ Y") — esto NO se tocó, sigue funcionando igual. Solo se quitó la
duplicación de arriba, liberando ~7mm de espacio vertical en cada
página y bajando el punto de inicio del contenido (y=37 → y=30).

================================================================================
================================================================================
==========

[v2.0715.2224 · 2026-07-15]
index.html, sw.js

Fix — bug crítico de interés "fantasma" en el pool LAV cuando hay huecos
de meses sin depósitos (imputarAbonosArt1595 / bloque "DEPÓSITOS LAV" en
calculate()). Detectado contra la liquidación oficial PJUD (RIT
Z-617-2024, caso "Test1"): el remanente de Sep-2025 salía en 0,2260 UTM
($15.652) en vez de los 0,1851 UTM ($12.821) oficiales — diferencia de
~$2.900 que se arrastraba al total final ($1.710.309 vs $1.707.929
oficial en la liquidación de referencia).

CAUSA RAÍZ (confirmada por simulación matemática contra los 19 depósitos
reales del caso, incluyendo el abono "Otros Abonos" del 05-03-2024):
cuando un depósito LAV llega con varios meses de hueco desde el
vencimiento de la cuota que finalmente alcanza a tocar (en este caso, el
depósito de 06-02-2026 tocando la cuota remanente de Sep-2025, con 154
días de diferencia), el pool calculaba interés de mora sobre esos 154
días ANTES de aplicar capital — consumiendo parte del depósito en
"interés fantasma" en vez de reducir el capital pendiente. El PJUD no
cobra interés en cada paso intermedio del consumo: aplica los depósitos
íntegros a capital en orden cronológico (Art. 1595 CC), y el interés
real de lo que finalmente queda sin cubrir se calcula UNA SOLA VEZ, al
final, desde el vencimiento de la cuota hasta la fecha de liquidación
(no hasta la fecha del depósito parcial que la tocó).

FIX (dos cambios en el mismo bloque, PASO 1 del pool LAV):
1. El bucle de consumo cronológico del pool ya NO calcula ni descuenta
   interés intermedio (`d._intPendUTM`) — solo aplica los depósitos a
   capital pendiente (`d._capDisponUTM`), en el mismo orden de siempre.
2. El post-proceso que arma `d.cap`/`d.inte` finales ya NO resta el
   interés intermedio acumulado del interés original (que estaba
   calculado hasta hoy sobre el capital BRUTO). En vez de eso, recalcula
   el interés final directamente sobre `capPostLavUTM` (el capital real
   que queda sin cubrir tras el pool), desde el vencimiento de la cuota
   hasta la fecha de liquidación — igual metodología que ya usaba
   correctamente el path "sin abonos ordinarios" más arriba en el mismo
   archivo.

VERIFICADO: simulación completa en Python replicando el algoritmo
corregido contra los 19 depósitos del caso Test1 (18 LAV + 1 "Otros
Abonos") reproduce EXACTO el capital remanente oficial en todos los
períodos: Sep-2025 = 0,18511 UTM (306 días mora), Oct-2025 a Jul-2026 =
2,33 UTM cada uno con sus días de mora correspondientes — coincide con
la tabla "II. Detalle liquidación" de la liquidación oficial PJUD RIT
Z-617-2024 punto por punto.

NOTA: el "Total cuotas impagas" del PDF (bug #1 mencionado por el
usuario, suma nominal histórica en vez de reajuste UTM por período) NO
se tocó en esta entrega — sigue pendiente, es un bug distinto en
export.js (ya parcialmente corregido en v2.0713.0237 según historial).

--------------------------------------------------------------------------------

index.html

UI — Panel de debug (`dbg()` / `_dbgPanel`): se eliminaron los botones
"📤 EXPORTAR DATOS", "✕ CERRAR", "🔄 RESET COLA SYNC" y "📊 ESTADO",
dejando ÚNICAMENTE el botón "📋 COPIAR". Causa: en pantallas angostas de
Android la fila de 5 botones sticky desbordaba horizontalmente sin
scroll, empujando "COPIAR" fuera del área visible — el usuario reportó
que el botón agregado en una versión anterior "no se ve". Con un solo
botón, ya no hay desborde. El botón flotante "📋 Copiar log" (fijo en la
esquina inferior derecha, independiente de la fila) se mantiene sin
cambios como respaldo adicional.

================================================================================
================================================================================
==========

[v2.0715.2223 · 2026-07-15]
export.js

Fix — falso positivo en la validación de suma OCR (v2.0715.0920).

El usuario probó la validación en vivo con el log de debug + botón
COPIAR (ambos agregados en versiones anteriores) y encontró dos
resultados:
1. Intento 1: el OCR SÍ cometió el error real (fila 02-08-2024 leída
   como $150.000 en vez de $155.000) — la validación lo detectó
   correctamente con una diferencia enorme.
2. Intento 2: el OCR leyó las 18 filas de la sección LAV correctamente
   (coinciden con el PJUD real), pero la validación IGUAL marcó una
   discrepancia de $140.000 — un FALSO POSITIVO.

Causa del falso positivo: el prompt le pedía al modelo comparar contra
"el Total de la sección V" asumiendo que el documento fuente siempre
separa las secciones IV (Otros Abonos) y V (Abonos LAV) como lo hace
el PDF oficial del PJUD. Pero cuando se reimporta un PDF que la propia
app generó (formato "ABONOS LAV" con AMBAS secciones combinadas en
una sola tabla con un único TOTAL en pesos), no existe un total de
"solo sección V" para comparar — el modelo tuvo que adivinar cómo
repartir el total combinado, y se equivocó.

Fix: la validación ahora compara por UTM en vez de pesos. La columna
UTM (o "Equiv. UTM") está SIEMPRE presente en ambas secciones,
independiente del formato del documento (oficial PJUD con secciones
separadas, o PDF propio de la app con tabla combinada). El prompt se
actualizó para:
- Pedir monto_utm en TODAS las filas (antes solo se pedía para
  "otros_abonos"; ahora también para "LAV", ya que esa columna
  también está impresa en ambos formatos).
- Reportar totales_documento con seccion_V_total_utm /
  seccion_IV_total_utm SI el documento las separa, O total_combinado_
  utm SI el documento trae un único total para toda la tabla — nunca
  ambos a la vez.
La validación en JS ahora detecta cuál de los dos casos aplica y
compara con la fórmula correcta en cada uno, evitando el falso
positivo sin perder la detección real (verificado que el caso 1 —
bug real — sigue siendo detectado con este cambio, ya que ambos
formatos usan UTM y el error de $155k→$150k se refleja igual de
claro en UTM: 2.35201 vs 2.27614).

También se corrigió una duplicación accidental de dos líneas al final
del prompt (introducida en el edit anterior) que rompía la sintaxis
del archivo — detectada y corregida antes de empaquetar.

================================================================================
================================================================================
==========

[v2.0715.0920 · 2026-07-15]
export.js

Feature — debug detallado + validación de suma en importación OCR de
LAV (modo PJUD), en respuesta al bug de montos desalineados detectado
en el caso "Test1" (ver entradas anteriores del changelog).

1. LOG DETALLADO: cada fila que el modelo de OCR devuelve ahora se
   registra en el panel de debug (dbg()) ANTES de cualquier
   procesamiento — fecha, monto y sección tal como llegó del modelo.
   Esto permite, la próxima vez que ocurra una desalineación, comparar
   directamente el log contra el PDF fuente y determinar si el error
   viene de la lectura del modelo o de un paso posterior de la app.
   Usar el botón "📋 COPIAR" (v2.0715.0909) para extraer el log
   completo y compararlo con calma.

2. VALIDACIÓN DE SUMA: se modificó el prompt del modo PJUD para pedir
   también el "Total" que el propio documento imprime al final de las
   secciones IV y V (campo totales_documento en la respuesta). La app
   ahora compara la suma de los montos extraídos contra ese total
   reportado por el modelo; si no coinciden, se muestra un aviso claro
   ANTES de continuar a la vista previa, con opción de cancelar y
   revisar el documento manualmente. Esto no identifica CUÁL fila está
   mal, pero evita que una desalineación pase desapercibida y
   contamine el cálculo silenciosamente — que es exactamente lo que
   ocurrió con "Test1".

Cambio de formato de la respuesta del modelo en modo PJUD: pasó de un
array plano a un objeto {movimientos, totales_documento}. Se actualizó
el parseo para manejar ambos formatos (PJUD: objeto; cartola bancaria:
sigue siendo array plano, sin total consolidado que validar). El resto
del flujo (_ocrResultados, ocrConfirmar, etc.) no cambió — sigue
operando sobre el array de movimientos como antes.

No afecta el motor de cálculo ni la imputación LAV — cambio acotado al
flujo de importación OCR.

================================================================================
================================================================================
==========

[v2.0715.0909 · 2026-07-15]
index.html

Mejora — panel de debug (dbg()): se agregó botón "📋 COPIAR" junto a
los botones existentes (EXPORTAR DATOS, CERRAR, RESET COLA SYNC,
ESTADO). Copia al portapapeles todas las líneas de log visibles en el
panel, en el orden en que aparecen, separadas por salto de línea.

Usa navigator.clipboard.writeText() con fallback a
document.execCommand('copy') vía <textarea> oculto, para navegadores
o webviews donde la Clipboard API no esté disponible. Da feedback
visual breve en el propio botón ("✅ COPIADO" / "❌ ERROR") en vez de
un alert().

Motivo: al reportar bugs (ej. el caso de desalineación de abonos LAV
importados por OCR), no había forma rápida de extraer el log de debug
completo para compartirlo — había que transcribirlo a mano desde la
pantalla. Con este botón, el flujo para reportar un problema es: abrir
panel de debug → reproducir el bug → 📋 COPIAR → pegar el log donde
se necesite.

No afecta el motor de cálculo, PDF, ni ningún flujo de datos — cambio
aislado a la UI del panel de debug.

================================================================================
================================================================================
==========

[v2.0715.0856 · 2026-07-15]
export.js

Mejora — el PDF de liquidación ahora incluye la versión de la app que
lo generó ("Pension UTM Pro vX.XXXX.XXXX"), leída del mismo
<meta name="app-version"> que usa el resto de la app. Se muestra en
dos lugares:
1. Bajo la línea "Generado: ... | UTM ref: ..." en la cabecera de la
   primera página.
2. En el pie de página, junto a "Pension UTM Pro – Calculo
   referencial", en TODAS las páginas del documento.

Motivo: al comparar PDFs de un mismo caso generados en distintos
momentos (ej. antes/después de un fix), no había forma de saber qué
versión del motor de cálculo produjo cada documento. Esto complicó el
diagnóstico del bug de desalineación de abonos LAV importados por OCR
(caso "Test1" vs "Test") — no se podía distinguir a simple vista si
la diferencia venía de datos de entrada distintos o de una versión
distinta del cálculo. Con el sello de versión, futuras comparaciones
entre PDFs permiten aislar esa variable de inmediato.

No afecta el motor de cálculo ni la imputación LAV — cambio
puramente de presentación en el PDF.

================================================================================
================================================================================
==========

[v2.0714.1015 · 2026-07-14]
index.html

FIX DE REGRESIÓN — la versión v2.0714.0955 (fix del pool LAV general)
introdujo un bug nuevo, más grave que el original, detectado por el
usuario comparando dos PDFs:

1. Caso "TEST BBVA" (antes correcto: $1.707.071 / 23,82547 UTM) pasó a
   dar $1.866.068 / 26,04458 UTM, con una línea nueva alarmante: "LAV
   remanente sin imputar (saldo a favor) +$148.566" — el único abono
   del caso dejó de aplicarse por completo.

2. Caso "Test1" empeoró de 23,976 UTM (ya desviado del PJUD) a 26,520
   UTM — más lejos aún del valor oficial (23,837 UTM) — con un símbolo
   "‡" nuevo en Ago-2025 señalando interés cubierto por "excedente
   LAV", indicio de doble cómputo de interés.

Causa: la condición `if (d._fechaVenc > fechaLav) break;` comparaba la
fecha EXACTA de vencimiento de la cuota (día 5, por ejemplo) contra la
fecha EXACTA del depósito. Un depósito que llega el día 2 del mes —
antes del día 5 de vencimiento de la cuota de ESE MISMO MES— hacía
que la condición se cumpliera y cortara el pool completo con `break`,
dejando el abono entero sin imputar (caso TEST BBVA, depósito
2025-09-02 vs. vencimiento 2025-09-05) y arrastrando remanentes
artificiales a los meses siguientes con recómputo de interés (caso
Test1).

Fix: se reemplaza la comparación por fecha exacta por comparación de
PERÍODO (año×12+mes de la cuota vs. año×12+mes del depósito). Un
depósito de septiembre ahora sí puede cubrir la cuota de septiembre
(o anteriores), independiente del día exacto dentro del mes. También
se ajustó el cálculo de interés pendiente para que solo se compute
cuando `fechaLav > fechaVenc` (evita interés negativo o espurio
cuando el depósito llega antes del vencimiento formal).

Verificado con arnés Node.js aislado (solo la lógica de puntero/gate,
sin depender de tablas de tasas CMF): el caso TEST BBVA ahora deja el
remanente correcto de 0,18511 UTM en su única cuota, en vez de 0
aplicado. PENDIENTE: el usuario debe recalcular ambos PDFs con esta
versión para confirmar visualmente los totales finales — no fue
posible re-simular con las tasas CMF reales fuera del motor vivo de
la app, así que esta validación es estructural (el gate), no
numérica end-to-end.

================================================================================
================================================================================
==========

[v2.0714.0955 · 2026-07-14]
index.html

Fix crítico — PASO 1 (pool LAV general): imputación incorrecta cuando
existen depósitos "Otros Abonos" (Sección IV del PJUD) mezclados con
depósitos LAV normales (Sección V) en pensiones mensuales ordinarias.

Detectado comparando el caso "Test1" (RIT Z-617-2024, 2do Juzgado de
Familia Talca) contra la liquidación oficial real del PJUD aportada
por el usuario: la app mostraba Sep-2025 con capital remanente de
$22.932 (0,331 UTM) mientras el PJUD calcula $12.822/$13.246 (0,18511
UTM) para el mismo mes — una diferencia de 0,138 UTM (~$9.900) que se
arrastraba hasta el total final (23,976 UTM app vs 23,837 UTM PJUD).

Causa raíz: el pool LAV general calculaba el interés a cubrir de CADA
cuota mensual usando SIEMPRE la fecha del PRIMER depósito LAV
cronológico (variable única _fechaPrimerLav), en vez de la fecha del
depósito que realmente alcanza a cubrir esa cuota específica. Esto
trataba todo el pool como si hubiera llegado de una sola vez en la
fecha más antigua, distorsionando qué interés se consideraba "ya
devengado" al momento de cada imputación.

Fix: se reemplaza el pool monolítico por CONSUMO DEPÓSITO A DEPÓSITO
en orden cronológico estricto, con un único puntero de cuota que
avanza secuencialmente sin retroceder ni reiniciar por cada depósito
(coincide con el criterio ya aplicado por reasignarPeriodosLav(), que
pre-asocia cada depósito a su período de imputación en secuencia). En
cada paso: 1° se cubre el interés devengado por la cuota actual
(calculado UNA sola vez, la primera vez que un depósito la toca — no
se recomputa en depósitos posteriores que la vuelvan a tocar, evitando
cobrar interés dos veces sobre el mismo tramo), 2° se cubre capital
pendiente (Art. 1595 CC). El remanente de un depósito que no alcanza a
saldar una cuota completa queda para el próximo depósito, que retoma
exactamente esa misma cuota antes de avanzar.

`origen: 'otros_abonos'` sigue siendo puramente una etiqueta de
visualización (ver export.js línea ~2987) — el motor de cálculo nunca
distinguió por origen; el bug estaba en el manejo de fechas del pool,
no en un filtro que excluyera "Otros Abonos".

Verificado numéricamente (script Python aislado, réplica exacta del
algoritmo) contra el caso RIT Z-617-2024: con el fix, el remanente
parcial cae en Sep-2025 = 0,18511 UTM, coincidiendo con el valor
oficial del PJUD. Pendiente: regenerar el PDF de este caso con esta
versión para confirmar visualmente que el total final baja de 23,976
a 23,837 UTM y que Ago-2025 queda "Cubierto LAV" (como en el PJUD) en
vez de mostrar remanente.

No se modificó imputarAbonosArt1595() (PASO 2, abonos ordinarios) ni
calculateConsolidada() (Deuda Histórica Consolidada) — ambos ya
confirmados correctos por el usuario contra el caso "TEST BBVA" y
liquidaciones RIT Z-3764-2023.

================================================================================
================================================================================
==========

[v2.0714.0810 · 2026-07-14]
index.html

Cambio de metodología — Deuda Histórica Consolidada + depósitos LAV:
se reemplaza el modelo event-driven (amortización secuencial, LAV
aplicado en la fecha exacta de cada depósito, Art. 1595 CC evento a
evento) por un modelo mucho más simple, CONFIRMADO contra una
liquidación oficial real del tribunal (RIT Z-3764-2023, 2° Juzgado de
Familia San Miguel, liquidador Luis Alfonso Molina Zárate, PJUD,
09-02-2024 — documento aportado por el usuario, no una aproximación).

Hallazgo clave: el tribunal NO amortiza evento por evento. Para la
fila de deuda arrastre (equivalente a la "Consolidada" de esta app):

  Capital bruto: 31,04667 UTM
  Pool LAV (4 depósitos may-ago 2023): 3,63896 UTM
  "Adeudado" oficial: 27,40771 UTM

31,04667 − 3,63896 = 27,40771 UTM — coincide EXACTO. Es decir: el pool
LAV completo se resta de una sola vez del capital bruto, sin importar
la fecha de cada depósito individual. El interés se calcula sobre ese
capital ya neteado, por los días de mora COMPLETOS desde el
vencimiento hasta la liquidación (461 días, 05-11-2022 → 09-02-2024,
coincide exacto), con tasa segmentada mes a mes — la misma
calcularInteresSegmentado() que ya usa el resto de la app.

Verificación numérica con las tasas TIC_REAJ ya presentes en el código:
interés calculado 2,50871 UTM vs. 2,49163 UTM real → diferencia de
0,01708 UTM (~$1.099 CLP), consistente con redondeos menores
acumulados en los 16 tramos mensuales — no un error estructural.
Sensiblemente mejor que el modelo anterior (que directamente rompía el
resumen del PDF, ver entradas v2.0714.0700 y v2.0714.0730).

Cambios:

1. Se elimina calcularInteresConsolidadaAmortizada() (event-driven) y
se reemplaza por calcularInteresConsolidadaNeteada(): resta el pool
LAV completo del capital bruto en un solo paso
(capUTM = max(0, capitalInicial − poolLavUTM)), calcula
saldoAFavorUTM = max(0, poolLavUTM − capitalInicial) para el caso
borde en que el pool exceda la deuda, y corre un único
calcularInteresSegmentado() sobre el capital neto entre vencimiento y
fecha de liquidación.

2. calculateConsolidada() ahora suma el pool LAV del período de la
consolidada (_poolLavUTMConsolidada) y usa el modelo neteado si es > 0;
sin LAV en el período, sigue igual que siempre
(calcularInteresConsolidadaMensual).

3. El detalle de la consolidada simplifica los campos LAV: como el
modelo neteado no "cubre" un interés preexistente evento a evento
(recalcula el interés desde cero sobre el capital ya reducido), todo
el pool consumido se atribuye a capital (lavAplicadoUTM) y
lavIntAplicadoUTM queda en 0. intOriginal = inte (ya no hay bruto/neto
que reconciliar por separado, a diferencia del intento anterior).

Pendiente: la diferencia residual de ~$1.100 CLP en el interés debe
investigarse por separado (ver historial de sesiones anteriores sobre
el residual de ~$858 CLP en otro caso) — probablemente redondeo menor
en la tasa de algún mes puntual o en el corte de tramo. No bloquea el
uso de la app: la desviación es del orden de 0,02 UTM sobre un total
de 60+ UTM adeudadas (~0,03%).

================================================================================
================================================================================
==========

[v2.0714.0730 · 2026-07-14]
index.html

Fix — bug crítico detectado en PDF real (caso "Test", 09-02-2024): el
Resumen Final anulaba por completo el efecto de los abonos LAV cuando
estos caían dentro del período de la Deuda Histórica Consolidada.

Causa raíz: export.js calcula "LAV remanente sin imputar (saldo a
favor)" restando de lavTotalCLPmodal (todos los LAV) lo que suma desde
d.lavIntAplicadoUTM / d.lavAplicadoUTM en lastCalculationData — pero el
fix anterior (v2.0714.0700) hizo que la fila isConsolidada resolviera
sus LAV en calculateConsolidada() sin poblar esos dos campos (el PASO 1
la salta con "continue" por consolidadaYaAmortizada). Resultado: los
LAV que caían dentro del período de la consolidada quedaban restados
del subtotal, pero export.js los contaba como "no imputados" y los
sumaba de vuelta íntegros como saldo a favor. Efecto neto: el pago se
diluía en $0, como si nunca hubiera existido, en vez de reducir el
capital de la consolidada. En el caso "Test" (PDF adjunto), esto
generó un saldo a favor falso de $230.200 que además era imposible por
otra razón: la deuda consolidada (28,981 UTM) es muchísimo mayor que
el pool LAV total (3,639 UTM), por lo que la deuda jamás pudo agotarse
y no puede existir saldo a favor mientras exista deuda pendiente (Art.
1595 CC / criterio SITFA-PJUD).

Cambios en calcularInteresConsolidadaAmortizada() y calculateConsolidada():

1. La función de amortización ahora separa y devuelve por separado
lavIntConsumidoUTM y lavCapConsumidoUTM (antes solo devolvía la suma
combinada lavsConsumidosUTM).

2. El detalle de la consolidada ahora puebla esLav, lavAplicadoUTM,
lavAplicadoCLP, lavIntAplicadoUTM y lavIntAplicadoCLP — los mismos
nombres de campo que usa el PASO 1 para las cuotas mensuales — para
que export.js (PDF y modal de resumen) los sume automáticamente sin
necesitar lógica especial ni tocar export.js.

3. intOriginal del detalle de la consolidada ahora refleja el interés
bruto devengado (antes de que los LAV cubrieran parte de él), no el
neto post-amortización, consistente con el significado de ese campo en
el resto de las filas.

Verificado con Python: replicando el caso del PDF, el fix hace que los
4 depósitos LAV (may-ago 2023, total 3.63897 UTM) se apliquen como
capital real de la consolidada en vez de generar un saldo a favor
inexistente, ya que el capital de 28.981 UTM nunca se agota con un
pool de solo 3.639 UTM.

Pendiente: aún no se ha regenerado el PDF del caso "Test" con esta
versión para confirmar visualmente que la línea "LAV remanente sin
imputar" desaparece y que el capital de la consolidada baja en ~3.6
UTM. Recomendado antes de dar por cerrado el fix.

================================================================================
================================================================================
==========

[v2.0714.0700 · 2026-07-14]
index.html

Fix — Deuda Histórica Consolidada + depósitos LAV dentro de su propio
período: la consolidada ahora amortiza secuencialmente (Art. 1595 CC,
evento por evento) en vez de aplicar el pool LAV general de una sola vez
sobre un interés ya pre-calculado con tasa segmentada mensual fija.

Causa raíz: cuando existían depósitos LAV con fecha posterior al
vencimiento de la consolidada, el PASO 1 (pool LAV general) los
aplicaba sobre lastCalculationData tratando la fila "isConsolidada"
como una cuota mensual más — con su interés ya calculado por
calcularInteresConsolidadaMensual() asumiendo que el pago completo
llega recién hoy. Esto no reproduce el orden judicial confirmado (ver
artículo de imputación de pagos adjunto en la conversación): la deuda
más antigua se cubre primero, en el orden reajuste→interés→capital, y
el excedente solo pasa a saldo a favor cuando la deuda llega a cero.
Nunca pueden coexistir deuda pendiente y saldo a favor.

Cambios:

1. Nueva función calcularInteresConsolidadaAmortizada(): recorre
cronológicamente vencimiento → cada depósito LAV dentro del período →
fecha de liquidación. En cada depósito aplica el orden Art. 1595
(1° interés devengado hasta esa fecha vía calcularInteresSegmentado(),
2° capital pendiente) y continúa el devengo sobre el saldo resultante.
Si el pago excede la deuda total (capital + interés), el remanente
pasa a saldoAFavorUTM solo cuando ambos llegan a cero.

2. calculateConsolidada() ahora detecta si existen depósitos LAV con
fecha dentro de su período (vencimiento < fecha ≤ hoy) y, de haberlos,
usa la nueva amortización secuencial en vez de
calcularInteresConsolidadaMensual(). Sin LAV en el período, el
comportamiento no cambia. El detalle resultante ahora expone
capOriginal/capOriginalBruto (monto bruto antes de amortizar),
consolidadaYaAmortizada, lavIdsConsumidosPorConsolidada y
saldoAFavorUTM para que el resto del pipeline sepa qué ya fue resuelto.

3. PASO 1 (pool LAV general): se construye abonosLavParaPool excluyendo
los depósitos ya consumidos por la amortización propia de la
consolidada (evita doble consumo de los mismos LAV), y el loop salta
por completo cualquier fila isConsolidada marcada
consolidadaYaAmortizada, ya que esa fila resuelve sus propios LAV en
calculateConsolidada() y no debe volver a tocarse con el pool general.

Pendiente de verificación: falta confirmar con tasas CMF reales que el
interés de la consolidada en el caso RIT Z-3764-2023 se acerque a
2,49163 UTM (referencia del tribunal). La lógica evento-por-evento y el
manejo de saldo a favor están verificados de forma aislada (test con
tasa fija reproduce el ejemplo del artículo: interés cubierto primero,
luego capital, excedente a saldo a favor solo con deuda en cero), pero
no se ha corrido con datos reales de ese RIT en esta sesión.

================================================================================
================================================================================
==========

[v2.0714.0646 · 2026-07-14]
index.html

Bugfix — card "Deuda Histórica" (modo Monto Fijo / consolidada): el toggle
"Intereses" (id consolidadaAplicaIntereses) no producía ningún cambio en
el hero total ("Total Adeudado") al activarlo/desactivarlo.

Causa raíz (dos partes):

1. Inconsistencia metodológica: la Deuda Histórica Consolidada calculaba
su interés mensualizado (calcularInteresConsolidadaMensual) cortando cada
tramo mensual el día 1 de cada mes, mientras que TODO el resto de la app
(pensiones mensuales, Deuda Histórica modo "Cuotas", imputación de abonos
Art. 1595, pool LAV) corta los tramos el día de vencimiento configurado
(getDiaVencimiento(), por defecto día 5) vía calcularInteresSegmentado().
Dos criterios de corte distintos para el mismo concepto de "mes de mora"
podían aplicar la tasa TIC/TMC de un mes distinto y producir un interés
total ligeramente distinto sin motivo legal, y además hacían que el
"parche" natural para el bug del toggle (usar el interés ya calculado
por la card) introdujera una diferencia de varios miles de pesos respecto
al valor que ya se usaba con el toggle activado.

2. Bug del toggle en sí: imputarAbonosArt1595() — que corre siempre
dentro de calculate() y cuyo resultado (cuotasResultado) alimenta
directamente el hero total — recalculaba el interés de TODAS las cuotas,
incluida la de Deuda Histórica Consolidada, con su propia lógica
(calcularInteresSegmentado sobre capPendiente), en vez de respetar el
interés que calculateConsolidada() ya había calculado correctamente
considerando consolidadaData.aplicaIntereses (0 si el switch está
apagado) y consolidadaData.usaMaxima. Este recálculo paralelo ignoraba
por completo el estado del toggle, por lo que el hero total mostraba
siempre el mismo interés sin importar el switch.

Fix aplicado (en ese orden, ambos necesarios):
a) calcularInteresConsolidadaMensual() ahora corta los tramos mensuales
   en el día de vencimiento configurado (getDiaVencimiento()) en lugar
   del día 1 fijo — mismo criterio que el resto de la app. Con esto, el
   interés calculado para la consolidada con el toggle activado coincide
   exactamente con el que ya calculaba imputarAbonosArt1595 por su cuenta,
   eliminando la discrepancia entre ambos métodos.
b) En imputarAbonosArt1595() (los dos puntos donde se recalcula interés:
   el path sin abonos ordinarios, y el cálculo de saldoFinal en el path
   con abonos), si la cuota es la Deuda Histórica Consolidada
   (d.isConsolidada) y su capital no fue tocado por ningún abono
   (capPendiente === cap original), se usa directamente el interés ya
   calculado (d.inte) en vez de recalcularlo. Esto no afecta pensiones
   normales, Deuda Histórica modo Cuotas, ni la imputación de abonos —
   solo evita que se pise el resultado ya correcto de la consolidada.

Verificado: con el fix, el total del hero con el toggle "Intereses"
activado es idéntico al valor que ya se mostraba antes de este cambio
(no varía ni un peso); con el toggle desactivado, el hero ahora sí baja
al reflejar interés = 0; al reactivar el toggle, vuelve exactamente al
mismo valor original. No se modificó calculatePensiones,
calcularInteresSegmentado, el pool LAV, ni ningún otro cálculo de
pensiones/abonos ordinarios.

Pendiente: confirmar en un caso real con período de pensión activo +
Deuda Histórica Consolidada simultáneos que el hero y el resumen de
liquidación coinciden en ambos estados del toggle.

[v2.0714.0035 · 2026-07-14]
index.html

Cambio de UI — card "Deuda Histórica" (modo Monto Fijo / consolidada):

1. Se detectó que la card y el "Detalle del cálculo" (resumen) podían
mostrar montos de interés distintos para la misma deuda histórica,
aunque coincidieran en capital y días de mora. Causa raíz identificada:
la card calcula su propio interés UNA VEZ (dentro de
tryRegisterConsolidada(), al editar los campos de Deuda Histórica) y
queda como snapshot fijo en pantalla, mientras que el resumen recalcula
el interés cada vez que corre calculate() (por cualquier cambio en la
app) — incluyendo tasas TIC/TMC que pueden actualizarse en segundo plano
vía _fetchTicCMF(). Si el usuario cambiaba algo después de registrar la
deuda histórica, la card quedaba con un valor de interés desactualizado
respecto del resumen (que siempre es el correcto/vigente).

2. Decisión (a pedido de Aviox): en vez de sincronizar el cálculo de
interés de la card con el del resumen, se eliminó por completo la
visualización de "Interés acum." y "días de interés" de la card. La
card ahora muestra solo Capital y Total, y el Total se muestra tanto en
UTM como en CLP (antes solo en CLP). El resumen de liquidación sigue
siendo la única fuente de verdad para el detalle de interés/días de la
deuda histórica.

Detalle técnico:
- HTML: se quitó el bloque "Interés acum." (id consolidadaIntLabel) y su
  días (id consolidadaDiasMoraLabel) de la card. El bloque "Total deuda"
  ahora incluye una segunda línea con el total en UTM (nuevo id
  consolidadaTotalUtmLabel).
- JS (tryRegisterConsolidada): ya no escribe consolidadaIntLabel ni
  consolidadaDiasMoraLabel (elementos eliminados del DOM). Se agregó el
  cálculo del total en UTM (total CLP / UTM vigente) para
  consolidadaTotalUtmLabel.

No se modificó ningún cálculo (calcularInteresConsolidadaMensual,
calcularInteresSegmentado, calculatePensiones, imputarAbonosArt1595, el
pool LAV): el interés de la deuda histórica se sigue calculando igual
internamente para el Total y para el resumen — solo se dejó de
desglosar/mostrar por separado en la card. Pendiente: confirmar que la
card muestra Capital + Total (UTM y CLP) sin el bloque de interés, y que
el Total de la card coincide con el subtotal de Deuda Histórica del
resumen al momento de registrar el dato.
