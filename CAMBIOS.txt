CAMBIOS v112g-fechaliq-prefill → v112h-prefill-early
=====================================================
Fecha: 2026-06-18

FIX / CAMPO "CALCULAR HASTA"
──────────────────────────────
El prellenado con fecha de hoy se movió al inicio de window.onload,
ANTES del await fetchUTM(). Así el campo muestra la fecha de inmediato
sin esperar a que carguen los datos UTM (evita que en Android aparezca
vacío durante la carga inicial).

CAMBIO (index.html):
  El bloque IIFE de prellenado se reubicó como primera instrucción
  de window.onload, antes de "await fetchUTM()".

ARCHIVOS MODIFICADOS: index.html, sw.js
SERVICE WORKER: v172g → v172h

