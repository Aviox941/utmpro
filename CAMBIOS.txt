CAMBIOS v114 → v116-inline-clean
==================================================================================
Fecha: 2026-06-18

FIX: Tarjeta inline "Detalle del cálculo" — modo compacto sin elementos redundantes
──────────────────────────────────────────────────────────────────────────────────
Se refactorizó buildResumenContent(targetContainer, inlineMode) para aceptar:
  • targetContainer: contenedor DOM destino (por defecto #resumenContent)
  • inlineMode: boolean — cuando true, omite:
      - Bloque "Datos del Caso" (colapsable)
      - Franjas de sección "PERÍODO ACTUAL", "ABONOS LAV — DETALLE", etc.
      - Caja azul "Total a Pagar — Liquidación Final"
      - Nota al pie ("Los valores son referenciales...")

La tarjeta inline muestra solo:
  • Tabla de cuotas por columna (Período/UTM/Capital$/Días/Tasa/Interés/Subtotal)
  • Separadores de año
  • Abonos LAV detalle
  • Pagos parciales con remanentes
  • Resumen final (filas de totales)

El modal del botón del header sigue mostrando todo completo (inlineMode=false).

ARCHIVOS MODIFICADOS
─────────────────────
- export.js   (buildResumenContent acepta targetContainer e inlineMode)
- index.html  (llamada con buildResumenContent(contentEl, true))
- sw.js       (CACHE_NAME: pension-utm-v175)
