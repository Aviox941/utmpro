================================================================================
INSTRUCCIONES DE VERSIONADO
================================================================================
- Este archivo se SOBRESCRIBE en cada entrega. No se acumulan versiones aquí.
- Cada modificación debe actualizar:
    1. index.html  → <meta name="app-version" content="2.MMDD.vXX">
    2. index.html  → <p id="appVersionLabel"> con el mismo valor
    3. sw.js       → CACHE_NAME = 'pension-utm-vNNN'  (incrementar NNN)
    4. Este archivo CAMBIOS.txt con el detalle de qué se modificó
- La letra/sufijo de versión (ej. v56) debe coincidir siempre con el nombre
  del zip entregado (ej. v56.zip).

================================================================================
VERSIÓN ACTUAL
================================================================================
Fecha         : 12-06-2026
App version   : 2.0612.v56   (meta + appVersionLabel en index.html)
SW cache      : pension-utm-v123   (sw.js)
Archivos mod  : index.html, export.js, sw.js

================================================================================
CAMBIOS EN ESTA ENTREGA
================================================================================

1. FIX BUG RESET TARJETA TOTALES (export.js)
   - resetAllSilent() no ocultaba heroMetaRow ni heroParcialRow al resetear.
   - Añadidas las dos líneas classList.add('hidden') en resetAllSilent() en
     export.js, junto al bloque existente de reset de yearTabsCard/detailsList.
   - Ahora al eliminar la información de cálculo la tarjeta queda limpia.

2. ELIMINACIÓN DE "REMANENTE DESCUENTOS" (index.html)
   - Removido el bloque HTML heroRemanenteRow (Remanente descuentos) del
     detalle de cálculo derecho.
   - Eliminado el bloque JS que actualizaba heroRemanenteRow en el snapshot
     de totales.
   - Eliminada la referencia a heroRemanenteRow en el bloque reset inline.

3. REDISEÑO TARJETA IZQUIERDA - META INFO (index.html)
   - La línea única "Fecha liq: XX · UTM ref: $YY" se separó en dos líneas:
       • heroUtmRef  → "UTM ref: $71.506"  (arriba)
       • heroFechaLiq → "Fecha liq: 26-05-2026"  (abajo)
   - Eliminada la línea heroMeses ("X meses adeudados") de la tarjeta izquierda.
     Los meses siguen mostrándose en la casilla de detalle mensual (yearTabs).
   - Actualizado el bloque JS de hero meta para usar los nuevos IDs.

================================================================================
